# Document Change Log — Carpool Mobility Platform (CMP)

**Control Document — Chronological Change Record**

| Field | Value |
|---|---|
| Document ID | CMP-CTRL-CHANGELOG |
| Document Name | Document Change Log |
| Project Name | Carpool Mobility Platform |
| Project Code | CMP |
| Version | 4.2 |
| Status | Draft |
| Date | 2026-08-20 |
| Author | Documentation Manager (AI-assisted) |
| Reviewer | [TBD] |
| Approver | Project Owner |
| Classification | Internal |
| Brand | TBD |
| Previous Version | 4.1 (2026-08-20) |
| Related Documents | README.md, Documentation_Index.md, Documentation_Status.md, Glossary.md, Master_Traceability_Matrix.md |

---

## 1. Purpose

This log records every change made to any document in the CMP documentation
repository: creation, revision, review, approval, supersession, and deprecation. It is
the audit trail for the documentation programme.

## 2. Recording Rules

1. Every document creation or revision produces exactly one entry.
2. Entries are append-only. Existing entries are never edited or deleted.
3. Entries are recorded newest-last (chronological order).
4. An approved version is never silently overwritten; a new version and a new entry are
   created instead.
5. Where a change resolves a conflict with an approved document, the entry must
   reference the conflict and the Project Owner decision that authorized it.

## 3. Change Log

| # | Date | Document ID | Document | Version | Change | Reason | Author | Approval Status |
|---|---|---|---|---|---|---|---|---|
| 001 | 2026-08-16 | CMP-REPO | Documentation Repository (structure) | 0.1 | Created `Document/` root and all 21 subdirectories (`00_Project_Control` … `20_Traceability_Release`). | Initial repository establishment per Master Documentation Control Prompt §9 and §35. | Documentation Manager (AI-assisted) | Not Submitted |
| 002 | 2026-08-16 | CMP-CTRL-README | Documentation Repository README | 0.1 | Created and initialized. Records project identity, technology direction, excluded technologies, architectural relationship, business authority principle, repository structure, generation rules, content integrity rules, change control, and current repository state. | Initial repository establishment per Master Documentation Control Prompt §10 and §35. | Documentation Manager (AI-assisted) | Not Submitted |
| 003 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.1 | Created and initialized. Registers 6 project-control documents and all 20 formal documents (DOC-01 … DOC-20) with status `Not Started`. Includes reserved filenames and requirement ID namespace allocation. | Initial repository establishment per Master Documentation Control Prompt §32. | Documentation Manager (AI-assisted) | Not Submitted |
| 004 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.1 | Created and initialized. All 20 formal documents set to `Not Started`. Includes status definitions, programme metrics, dependency graph, prerequisite readiness table, and next action. | Initial repository establishment per Master Documentation Control Prompt §33 and §36. | Documentation Manager (AI-assisted) | Not Submitted |
| 005 | 2026-08-16 | CMP-CTRL-CHANGELOG | Document Change Log | 0.1 | Created and initialized with entries 001–007 covering repository initialization. | Initial repository establishment per Master Documentation Control Prompt §34. | Documentation Manager (AI-assisted) | Not Submitted |
| 006 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.1 | Created and initialized with the controlled term set supplied in the Master Documentation Control Prompt §30, plus platform, role, and technology terminology. | Initial repository establishment per Master Documentation Control Prompt §30. | Documentation Manager (AI-assisted) | Not Submitted |
| 007 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.1 | Created and initialized. Trace chain model defined; no requirement links recorded (no formal documents exist yet). | Initial repository establishment per Master Documentation Control Prompt §31. | Documentation Manager (AI-assisted) | Not Submitted |
| 008 | 2026-08-16 | **CMP-DOC-01** | **Business Analysis Document (BAD)** | **0.1** | **Initial issue.** Created `DOC-01-BAD-CMP-Business-Analysis.md` (2,617 lines, 22 Mermaid diagrams) and `DOC-01-BAD-CMP-Business-Analysis.docx` (cover page, live TOC field, header/footer with page numbering, 121 captioned tables, 22 rendered diagrams). Covers all 33 sections mandated by Master Documentation Control Prompt §38. Issues 78 business requirements (`BAD-BR-001`…`BAD-BR-078`) plus auxiliary registers: 10 objectives, 14 success criteria, 14 stakeholders, 7 personas, 12 processes, 42 business rules, 32 capabilities, 24 risks, 18 constraints, 14 dependencies, 10 opportunities, 26 KPIs, 15 assumptions, 31 open questions, 24 required business decisions. | Explicit Project Owner command: **"Create BAD"**. | Business Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 009 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.2 | Updated DOC-01 to v0.1/Draft with file locations; marked DOC-01 filenames as created; recorded `BAD-BR-001`…`BAD-BR-078` as issued; added §5.1 registering the 15 auxiliary identifier prefixes introduced by CMP-DOC-01; updated control-file version rows. | Workflow Step 12 following creation of CMP-DOC-01. | Documentation Manager (AI-assisted) | Not Submitted |
| 010 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.2 | Updated programme metrics (19 Not Started, 1 Draft); DOC-01 set to v0.1 Draft; DOC-02 prerequisite readiness qualified; rewrote §7 Next Action; added §7.1 readiness warning for DOC-02 and §7.2 outstanding-items summary. | Workflow Step 13 following creation of CMP-DOC-01. | Documentation Manager (AI-assisted) | Not Submitted |
| 011 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.2 | Added three terms introduced by CMP-DOC-01 Appendix B (**Corridor**, **Corridor Liquidity**, **Commute Loop**); added the BAD identifier-prefix terms to the documentation-terms section; linked six existing open questions to their `BAD-DEC-` decision IDs. | Workflow Step 15 following creation of CMP-DOC-01. | Documentation Manager (AI-assisted) | Not Submitted |
| 012 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.2 | Recorded 78 `BAD-BR-` requirements as defined with 0 traced forward (`TRACEABILITY: TBD` — no downstream document exists); updated coverage summary; added Gap Register entries GAP-001 and GAP-002. No traceability links fabricated. | Workflow Step 16 following creation of CMP-DOC-01. | Documentation Manager (AI-assisted) | Not Submitted |
| 013 | 2026-08-16 | **CMP-DOC-02** | **Business Requirements Document (BRD)** | **0.1** | **Initial issue.** Created `DOC-02-BRD-CMP-Business-Requirements.md` (7 Mermaid diagrams) and `DOC-02-BRD-CMP-Business-Requirements.docx` (cover page, live TOC field, header/footer with page numbering, 70 captioned tables, 7 rendered diagrams). Elaborates the 78 `BAD-BR` requirements of CMP-DOC-01 into **188 business requirements** (`BRD-REQ-001`…`BRD-REQ-188`) across 15 domains, plus 32 stakeholder requirements, 26 data requirements, 18 reporting requirements, 14 interface requirements and 12 compliance requirements — **368 requirement statements in total**. Records 109 Ready / 79 Blocked, 24 absolute-rule requirements, 17 blocking decisions, 10 assumptions, 6 constraints, 8 dependencies, 10 risks and 12 open questions. Backward traceability complete: 78 of 78 `BAD-BR` covered, 0 orphans. | Explicit Project Owner command: **"Create BRD"**, issued after the author's recommendation (CMP-DOC-01 `R-03`) to resolve the §27 decisions first. **Project Owner directed to proceed; recommendation recorded and superseded by that direction.** | Business Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 014 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.3 | Updated DOC-02 to v0.1/Draft with file locations; marked DOC-02 filenames as created; recorded `BRD-REQ-001`…`BRD-REQ-188` as issued; added §5.2 registering the 10 auxiliary identifier prefixes introduced by CMP-DOC-02; updated control-file version rows. | Workflow Step 12 following creation of CMP-DOC-02. | Documentation Manager (AI-assisted) | Not Submitted |
| 015 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.3 | Updated programme metrics (18 Not Started, 2 Draft, 10% drafted); DOC-02 set to v0.1 Draft; DOC-03 prerequisite readiness qualified as partial; rewrote §7 Next Action for DOC-03; replaced §7.1 with a two-constraint readiness position; expanded §7.2 to cover the whole chain. | Workflow Step 13 following creation of CMP-DOC-02. | Documentation Manager (AI-assisted) | Not Submitted |
| 016 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.3 | Added the five documentation terms introduced by CMP-DOC-02 Appendix B (**Ready**, **Blocked**, **Verification intent**, **Absolute-rule requirement**, **MoSCoW**) and the `BRD-REQ-` identifier term. | Workflow Step 15 following creation of CMP-DOC-02. | Documentation Manager (AI-assisted) | Not Submitted |
| 017 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.3 | **First real traceability links recorded.** All 78 `BAD-BR` requirements now trace forward to one or more of the 188 `BRD-REQ` requirements (100% forward coverage at this level); 188 `BRD-REQ` recorded as defined with 0 traced forward to CMP-DOC-03 (`TRACEABILITY: TBD`). Updated coverage summary; closed GAP-001 for the BAD level; added GAP-003 and GAP-004. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-02. | Documentation Manager (AI-assisted) | Not Submitted |

| 018 | 2026-08-16 | **CMP-DOC-03** | **Stakeholder & Use Case Specification** | **0.1** | **Initial issue.** Created `DOC-03-USECASE-CMP-Stakeholders-UseCases.md` (4,001 lines, 7 Mermaid diagrams) and `DOC-03-USECASE-CMP-Stakeholders-UseCases.docx` (cover page, live TOC field, header/footer with page numbering, 140 captioned tables, 7 rendered diagrams). Defines **14 actors** (`ACT-01`…`ACT-14`) and **83 use cases** (`UC-001`…`UC-083`) across 15 packages, in three specification tiers: **44 Specified, 6 Partial, 33 Outlined**. Maps all 32 stakeholder requirements to realising use cases. Realises 184 of 188 `BRD-REQ`; the remaining 4 are justified as constraints rather than interactions. All 10 absolute business rules are enforced by named, fully specified use cases. Records 7 assumptions, 8 risks and 10 open questions. **No flow was written for behaviour governed by an unresolved decision.** | Explicit Project Owner command: **"Create Use Cases"**, issued while both predecessors remain `Draft` and 42% of the requirement base is blocked. **Project Owner directed to proceed; the author's recommendation to resolve decisions first is recorded and superseded by that direction.** | Business Analyst / Product Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 019 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.4 | Updated DOC-03 to v0.1/Draft with file locations; marked DOC-03 filenames as created; recorded `UC-001`…`UC-083` as issued; added §5.3 registering the 4 auxiliary prefixes introduced by CMP-DOC-03; updated repository totals to 349 traceable identifiers and 451 statements. | Workflow Step 12 following creation of CMP-DOC-03. | Documentation Manager (AI-assisted) | Not Submitted |
| 020 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.4 | Updated programme metrics (17 Not Started, 3 Draft, 15% drafted); DOC-03 set to v0.1 Draft; DOC-04 prerequisite readiness qualified as partial; rewrote §7 Next Action for DOC-04; updated §7.2 outstanding items. | Workflow Step 13 following creation of CMP-DOC-03. | Documentation Manager (AI-assisted) | Not Submitted |
| 021 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.4 | Added the six specification terms introduced by CMP-DOC-03 Appendix C (`UC-nnn`, **Specified/Partial/Outlined**, **Blocked step**, the four **actor classes**, **Implicit precondition**, **Integrity-critical use case**). | Workflow Step 15 following creation of CMP-DOC-03. | Documentation Manager (AI-assisted) | Not Submitted |
| 022 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.4 | Recorded the `BRD-REQ → UC` link: 184 of 188 requirements realised by a use case, 4 justified as non-interactions (100% accounted). 83 `UC` recorded as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-04 does not exist). Closed GAP-004; added GAP-006 and GAP-007. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-03. | Documentation Manager (AI-assisted) | Not Submitted |

| 023 | 2026-08-16 | **CMP-DOC-04** | **Functional Requirements Document (FRD)** | **0.1** | **Initial issue.** Created `DOC-04-FRD-CMP-Functional-Requirements.md` (1,408 lines, 3 Mermaid diagrams) and `DOC-04-FRD-CMP-Functional-Requirements.docx` (cover page, live TOC field, header/footer with page numbering, 57 captioned tables, 3 rendered diagrams). Decomposes the **50 use cases carrying written flows** into **260 functional requirements** (`FRD-FR-001`…`FRD-FR-260`) across 12 functional areas, each carrying a source use case, business requirement, MoSCoW priority and verification method (211 Test, 23 Demonstration, 23 Inspection, 3 Analysis). Marks **81 integrity-critical requirements (‡)**. **The 33 Outlined use cases are NOT decomposed**; 29 functional gaps (`FRD-GAP-001`…`029`, 11 Critical) are recorded in their place with the decision required. Three functional areas — Ratings & Reviews, Wallet & Rewards, Recurring Commute — contain zero requirements by decision. Records 7 assumptions, 9 risks, 10 open questions. | Explicit Project Owner command: **"Create FRD"**, issued while all three predecessors remain `Draft` and 33 use cases remain Outlined. **Project Owner directed to proceed; the author's recommendation to resolve decisions first is recorded and superseded by that direction.** | Product Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 024 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.5 | Updated DOC-04 to v0.1/Draft with file locations; marked DOC-04 filenames as created; recorded `FRD-FR-001`…`FRD-FR-260` as issued; added §5.4 registering the 4 auxiliary prefixes introduced by CMP-DOC-04; updated repository totals to 609 traceable identifiers and 711 statements. | Workflow Step 12 following creation of CMP-DOC-04. | Documentation Manager (AI-assisted) | Not Submitted |
| 025 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.5 | Updated programme metrics (16 Not Started, 4 Draft, 20% drafted); DOC-04 set to v0.1 Draft; DOC-05 marked as the recommended next document and largely independent of the open decisions; rewrote §7 Next Action; updated §7.2 outstanding items. | Workflow Step 13 following creation of CMP-DOC-04. | Documentation Manager (AI-assisted) | Not Submitted |
| 026 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.5 | Added the five specification terms introduced by CMP-DOC-04 Appendix C (`FRD-FR-nnn`, **Functional gap**, **Verification method**, **Integrity-critical requirement**, **Live product defect**). | Workflow Step 15 following creation of CMP-DOC-04. | Documentation Manager (AI-assisted) | Not Submitted |
| 027 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.5 | Recorded the `UC → FRD-FR` link: 50 of 83 use cases decomposed (60%), 33 withheld pending decisions. 260 `FRD-FR` recorded as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-06 does not exist). Closed GAP-007; added GAP-010 and GAP-011. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-04. | Documentation Manager (AI-assisted) | Not Submitted |

| 028 | 2026-08-16 | **CMP-DOC-05** | **Non-Functional Requirements / Quality Attributes (NFR)** | **0.1** | **Initial issue.** Created `DOC-05-NFR-CMP-Non-Functional-Requirements.md` (4 Mermaid diagrams) and `DOC-05-NFR-CMP-Non-Functional-Requirements.docx` (cover page, live TOC field, header/footer with page numbering, 58 captioned tables, 4 rendered diagrams). Defines **13 quality attributes** and **162 quality requirements** (`NFR-001`…`NFR-162`), each with a measurable metric, a named measurement point, a priority and a verification method (83 Test, 59 Inspection, 13 Demonstration, 7 Analysis). **No target value is invented**: 44 are Absolute (rule-derived, marked ‡ and listed in Appendix B), 49 carry stated non-numeric targets, 47 await `BAD-DEC-018` and 22 await a technical decision — **93 of 162 (57%) are enforceable without any further decision**. Adds two quality attributes not previously expressed anywhere in the chain: **Cost Efficiency** (metered third-party cost per trip, `BAD-RISK-013`) and **Mobile Resource Consumption** (battery and data on the user's own device). Identifies 4 attribute trade-offs with named owners, and supplies a target-setting framework (§19). Records 7 assumptions, 10 risks and 10 open questions. | Explicit Project Owner command: **"Create NFR"**, following the author's recommendation in CMP-DOC-04 `FR-09` that NFR precede the SRS. | Solution Architect (AI-assisted) | **Not Submitted — Draft for review** |
| 029 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.6 | Updated DOC-05 to v0.1/Draft with file locations; marked DOC-05 filenames as created; recorded `NFR-001`…`NFR-162` as issued; added §5.5 registering the 4 auxiliary prefixes introduced by CMP-DOC-05; updated repository totals to 771 traceable identifiers and 873 statements. | Workflow Step 12 following creation of CMP-DOC-05. | Documentation Manager (AI-assisted) | Not Submitted |
| 030 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.6 | Updated programme metrics (15 Not Started, 5 Draft, 25% drafted); DOC-05 set to v0.1 Draft; DOC-06 readiness upgraded — both its prerequisites now exist; added quality-requirement metrics to §7.2. | Workflow Step 13 following creation of CMP-DOC-05. | Documentation Manager (AI-assisted) | Not Submitted |
| 031 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.6 | Added the five terms introduced by CMP-DOC-05 Appendix C (**Quality attribute**, **Absolute target**, **Measurement point**, **Target-setting route**, **Quality trade-off**) and the `NFR-nnn` identifier term. | Workflow Step 15 following creation of CMP-DOC-05. | Documentation Manager (AI-assisted) | Not Submitted |
| 032 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.6 | Recorded 162 `NFR` requirements as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-06 does not exist). Recorded that the NFR **qualifies** the FRD rather than succeeding it in the chain. Added GAP-012. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-05. | Documentation Manager (AI-assisted) | Not Submitted |

| 033 | 2026-08-16 | **CMP-DOC-06** | **Software Requirements Specification (SRS)** | **0.1** | **Initial issue.** Created `DOC-06-SRS-CMP-Software-Requirements.md` (4 Mermaid diagrams) and `DOC-06-SRS-CMP-Software-Requirements.docx` (cover page, live TOC field, header/footer with page numbering, 73 captioned tables, 4 rendered diagrams). Defines a **six-element software model** (`SRS-EL-01`…`06`), **allocates all 260 functional and 162 quality requirements** to an accountable element (100%, 0 orphans), and states **184 software-level requirements** (`SRS-REQ-001`…`SRS-REQ-184`) covering element obligations, external interfaces, state and lifecycle, error handling and configuration. Marks **84 integrity-critical requirements (‡)**. Identifies **4 trust boundaries** and records that only one was previously well defended. Establishes the **consolidated verification baseline: 606 requirements, 209 integrity-critical**. Discloses 6 requirements with no upstream counterpart (§11.5). Records 10 state models of which 6 are undefined pending decisions, 29 functional gaps with a pre-assigned element, 3 reserved areas, 8 technical decisions routed to architecture, 7 assumptions, 10 risks and 10 open questions. | Explicit Project Owner command: **"Create SRS"**, following the author's recommendation in CMP-DOC-05 `NR-10`. | Solution Architect (AI-assisted) | **Not Submitted — Draft for review** |
| 034 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.7 | Updated DOC-06 to v0.1/Draft with file locations; marked DOC-06 filenames as created; recorded `SRS-REQ-001`…`SRS-REQ-184` as issued; added §5.6 registering the 4 auxiliary prefixes introduced by CMP-DOC-06; updated repository totals to 955 traceable identifiers and 1,057 statements. | Workflow Step 12 following creation of CMP-DOC-06. | Documentation Manager (AI-assisted) | Not Submitted |
| 035 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.7 | Updated programme metrics (14 Not Started, 6 Draft, 30% drafted); DOC-06 set to v0.1 Draft; DOC-07 readiness upgraded — allocation complete, 8 technical decisions routed to it, sizing limited by unset quality targets; added software-requirement metrics to §7.2. | Workflow Step 13 following creation of CMP-DOC-06. | Documentation Manager (AI-assisted) | Not Submitted |
| 036 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.7 | Added the seven terms introduced by CMP-DOC-06 Appendix C (`SRS-REQ-nnn`, **Software element**, **Accountable / Contributing**, **Trust boundary**, **Interaction rule**, **Reserved area**, **Structural verification**). | Workflow Step 15 following creation of CMP-DOC-06. | Documentation Manager (AI-assisted) | Not Submitted |
| 037 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.7 | Recorded that CMP-DOC-06 allocates **100% of both the 260 `FRD-FR` and the 162 `NFR`** to an accountable software element — closing the forward-coverage gap at both levels. Recorded 184 `SRS-REQ` as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-07 does not exist). Closed GAP-011; added GAP-013. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-06. | Documentation Manager (AI-assisted) | Not Submitted |

| 038 | 2026-08-16 | **CMP-DOC-07** | **System Architecture Document (SAD)** | **0.1** | **Initial issue.** Created `DOC-07-SAD-CMP-System-Architecture.md` (14 Mermaid diagrams) and `DOC-07-SAD-CMP-System-Architecture.docx` (cover page, live TOC field, header/footer with page numbering, 79 captioned tables, 14 rendered diagrams). Establishes **12 architectural drivers**, **10 principles** and **24 Architecture Decision Records** — resolving **all 8 technical decisions routed from CMP-DOC-06 §13.2**. Issues **148 architecture statements** (`ARCH-001`…`ARCH-148`) covering system context, 5 containers, 31 components across all six software elements, 5 runtime views, and the data, integration, security and logical deployment architectures. Records **all 10 absolute business rules with a named architectural guarantee**, 4 driver conflicts resolved, 6 trade-offs, **11 sizing decisions deferred to CMP-DOC-19** with named blockers, 7 assumptions, 12 risks and 10 open questions. **`ADR-24` records the single structural exception**: safety incident handling is the only independently deployable capability, resolving the conflict between `ADR-01` (integrity) and `SRS-REQ-082` (safety availability). **No capacity, latency, availability or cost figure is stated**, because none has been approved. | Explicit Project Owner command: **"Create SAD"**, following the author's recommendation in CMP-DOC-06 `SR-10`. | Solution Architect (AI-assisted) | **Not Submitted — Draft for review** |
| 039 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.8 | Updated DOC-07 to v0.1/Draft with file locations; marked DOC-07 filenames as created; recorded `ARCH-001`…`ARCH-148` as issued; added §5.7 registering the 6 auxiliary prefixes introduced by CMP-DOC-07; updated repository totals to 1,103 traceable identifiers and 1,205 statements. | Workflow Step 12 following creation of CMP-DOC-07. | Documentation Manager (AI-assisted) | Not Submitted |
| 040 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.8 | Updated programme metrics (13 Not Started, 7 Draft, 35% drafted); DOC-07 set to v0.1 Draft; DOC-08, DOC-09 and DOC-13 readiness upgraded — all three may now proceed, the first two in parallel; added architecture metrics to §7.2; rewrote §7 Next Action. | Workflow Step 13 following creation of CMP-DOC-07. | Documentation Manager (AI-assisted) | Not Submitted |
| 041 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.8 | Added the eight terms introduced by CMP-DOC-07 Appendix C (`ARCH-nnn`, **Architectural driver**, **Architecture Decision Record**, **Port / adapter**, **Evidential log**, **Projection**, **Policy configuration**, **Sizing decision**, **Structural exception**). | Workflow Step 15 following creation of CMP-DOC-07. | Documentation Manager (AI-assisted) | Not Submitted |
| 042 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.8 | Recorded 148 `ARCH` statements as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-08 and CMP-DOC-09 do not exist). Recorded that 96 of the 184 `SRS-REQ` are realised by a named architecture statement. Added GAP-014 (11 sizing decisions). Updated GAP-013 to note that fraud remains unowned through the architecture. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-07. | Documentation Manager (AI-assisted) | Not Submitted |

| 043 | 2026-08-16 | **CMP-DOC-08** | **Mobile Architecture Document** | **0.1** | **Initial issue.** Created `DOC-08-MOBILE-CMP-Mobile-Architecture.md` (7 Mermaid diagrams) and `DOC-08-MOBILE-CMP-Mobile-Architecture.docx` (cover page, live TOC field, header/footer with page numbering, 58 captioned tables, 7 rendered diagrams). Establishes **10 mobile drivers** and **16 Mobile Architecture Decision Records**, and issues **168 statements** (`MOB-001`…`MOB-168`) covering module and layer structure, presentation, client domain, data, device capability, concurrency, offline, error handling, configuration and version negotiation, resource management, on-device security posture, testing and build. **All 14 client obligations from CMP-DOC-07 §6.1 are realised.** Two decisions carry most of the weight: `MADR-02` makes value **provenance** a type-level property so `NFR-086` is structural rather than remembered, and `MADR-05` separates the presentation cache from the outbox so the client can work offline while holding nothing authoritative. **No resource budget figure is stated** — 9 measurement points are named instead, since `NFR-151`–`158` are all `[TBD-BUS]`. Records 7 obligations placed on CMP-DOC-10 and CMP-DOC-12, 7 assumptions, 10 risks and 8 open questions. | Explicit Project Owner command: **"Create Mobile Architecture"**, following CMP-DOC-07 `AR-08`. | Software Architect — Android (AI-assisted) | **Not Submitted — Draft for review** |
| 044 | 2026-08-16 | CMP-CTRL-INDEX | Documentation Index | 0.9 | Updated DOC-08 to v0.1/Draft with file locations; marked DOC-08 filenames as created; recorded `MOB-001`…`MOB-168` as issued; added §5.8 registering the 5 auxiliary prefixes introduced by CMP-DOC-08; updated repository totals to 1,271 traceable identifiers and 1,373 statements. | Workflow Step 12 following creation of CMP-DOC-08. | Documentation Manager (AI-assisted) | Not Submitted |
| 045 | 2026-08-16 | CMP-CTRL-STATUS | Documentation Status | 0.9 | Updated programme metrics (12 Not Started, 8 Draft, 40% drafted); DOC-08 set to v0.1 Draft; DOC-12 readiness upgraded to Ready with the CMP-DOC-08 §17.5 obligations as input; DOC-15 readiness qualified; added mobile architecture metrics to §7.2; rewrote §7 Next Action. | Workflow Step 13 following creation of CMP-DOC-08. | Documentation Manager (AI-assisted) | Not Submitted |
| 046 | 2026-08-16 | CMP-CTRL-GLOSSARY | Glossary | 0.9 | Added the six terms introduced by CMP-DOC-08 Appendix C (`MOB-nnn`, **Provenance**, **Presentation cache**, **Outbox**, **Intent (client)**, **Trip-bound acquisition**, **Version negotiation**). | Workflow Step 15 following creation of CMP-DOC-08. | Documentation Manager (AI-assisted) | Not Submitted |
| 047 | 2026-08-16 | CMP-CTRL-RTM | Master Traceability Matrix | 0.9 | Recorded 168 `MOB` statements as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-12, CMP-DOC-15 and CMP-DOC-18 do not exist). Recorded that 46 `ARCH` statements are realised by CMP-DOC-08, including **14 of 14 client obligations**. Added GAP-015. No links fabricated. | Workflow Step 16 following creation of CMP-DOC-08. | Documentation Manager (AI-assisted) | Not Submitted |
| 048 | 2026-08-17 | **CMP-DOC-09** | **Backend Architecture Document** | **0.1** | **Initial issue.** Created `DOC-09-BACKEND-CMP-Backend-Architecture.md` (6 Mermaid diagrams) and `DOC-09-BACKEND-CMP-Backend-Architecture.docx` (cover page, live TOC field, header/footer with page numbering, 75 captioned tables, 6 rendered diagrams). Establishes **12 backend drivers** and **18 Backend Architecture Decision Records**, and issues **218 statements** (`BE-001`…`BE-218`) covering namespace and layer structure, the domain layer and its 9 aggregates, application services and transaction boundaries, both interface surfaces, persistence and repositories, the evidential log, projections, the 7-family work subsystem, 5 ports, policy configuration and state machines, authorisation and error handling, the safety surface, observability, and testing and structural enforcement. **All 24 SAD backend obligations are realised** and **all 10 absolute business rules have a named backend owner**. Three decisions carry most of the weight: `BADR-01` rejects framework-default MVC for domain-centric namespaces, `BADR-15` requires every Filament read and write to pass through an application service, and `BADR-18` makes 8 structural rules fail the build so the first two remain true after review ends. **No pricing, commission, threshold or retention value is invented** — §13.2 lists 11 tuning values as configuration precisely because 11 decisions are outstanding. Records 7 obligations placed on other documents, 6 assumptions, 8 risks and 7 open questions. | Explicit Project Owner command: **"Create Backend Architecture"**, following CMP-DOC-08 §7.1. | Software Architect — Backend (AI-assisted) | **Not Submitted — Draft for review** |
| 049 | 2026-08-17 | CMP-CTRL-INDEX | Documentation Index | 1.0 | Updated DOC-09 to v0.1/Draft with file locations; marked DOC-09 filenames as created; recorded `BE-001`…`BE-218` as issued; added §5.9 registering the 5 auxiliary prefixes introduced by CMP-DOC-09; updated repository totals to 1,489 traceable identifiers and 1,424 statements. | Workflow Step 12 following creation of CMP-DOC-09. | Documentation Manager (AI-assisted) | Not Submitted |
| 050 | 2026-08-17 | CMP-CTRL-STATUS | Documentation Status | 1.0 | Updated programme metrics (11 Not Started, 9 Draft, 45% drafted); DOC-09 set to v0.1 Draft; DOC-10, DOC-11 and DOC-17 readiness upgraded from Blocked to Ready following the removal of their DOC-09 dependency; added backend architecture metrics to §7.2; rewrote §7 Next Action for the post-architecture position. | Workflow Step 13 following creation of CMP-DOC-09. | Documentation Manager (AI-assisted) | Not Submitted |
| 051 | 2026-08-17 | CMP-CTRL-GLOSSARY | Glossary | 1.0 | Added the ten terms introduced by CMP-DOC-09 Appendix C (`BE-nnn`, **Aggregate**, **Application service**, **Absolute rule**, **Evidential record**, **Idempotency registry**, **Integrity-critical (‡)**, **Policy configuration**, **Port**, **Projection**, **`Unavailable`**). | Workflow Step 15 following creation of CMP-DOC-09. | Documentation Manager (AI-assisted) | Not Submitted |
| 052 | 2026-08-17 | CMP-CTRL-RTM | Master Traceability Matrix | 1.0 | Recorded 218 `BE` statements as defined with 0 traced forward (`TRACEABILITY: TBD` — CMP-DOC-10, CMP-DOC-11, CMP-DOC-17 and CMP-DOC-18 do not exist). Recorded that 24 `ARCH` statements are realised by CMP-DOC-09 and that all 10 absolute rules now have a backend owner, bringing `ARCH` forward-traced from 46 to 70 of 148. Added **GAP-016** (launch scale unset — three backend assumptions and CMP-DOC-11's index strategy depend on it). Added the `ADM-` cross-cutting row. **Correction applied:** versions 0.7–0.9 of this matrix carried two stale sentences asserting that CMP-DOC-06 and CMP-DOC-07 did not exist after both had been created; both are removed and the correction is disclosed in §4. No traceability link was affected and no link fabricated. | Workflow Step 16 following creation of CMP-DOC-09. | Documentation Manager (AI-assisted) | Not Submitted |
| 053 | 2026-08-17 | **CMP-DOC-09** | **Backend Architecture Document** | **0.2** | **Traceability correction — reissue.** A citation audit resolving every cited identifier back to the text of the statement it names found that **78 of 163 externally-verifiable citations in v0.1 did not correspond to the statement cited**. Identifiers had been written from recollection of what each ought to say rather than resolved against source, and the v0.1 quality check verified only that each identifier was numerically in range. **92 Src cells corrected.** Adds **§18.7** disclosing the 7 statements with no upstream counterpart, and **`BE-OQ-08`/`BE-OQ-09`** raising the 2 that are requirement-chain gaps. **No statement text, identifier, decision, count or diagram changed.** The same audit run against CMP-DOC-07 (10%) and CMP-DOC-08 (18%) found rates within the screen's false-positive band; both were spot-checked and no correction was required. | §21 prohibits fabricated traceability; §28 requires cross-document consistency. Defect found while preparing CMP-DOC-10. | Software Architect — Backend (AI-assisted) | **Not Submitted — Draft for review** |
| 054 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / RTM | Control files | 1.1 | DOC-09 advanced to v0.2; `BE-OQ-` count 7 → 9; recorded the 92 corrected citations, the 7 statements with no upstream counterpart and the 2 requirement-chain gaps; added constraint 6 to §7.1. | Workflow Steps 12–16 following the CMP-DOC-09 v0.2 reissue. | Documentation Manager (AI-assisted) | Not Submitted |
| 055 | 2026-08-17 | **Quality process** | §41 quality check | — | **Process change.** Every prior quality check verified that a cited identifier was *numerically in range*; none verified that the statement it named *said what was claimed*. That gap is why 78 wrong citations passed 116 checks. A reusable citation-resolution check (`xref_check`) is added to the quality-check suite and is to be run for every subsequent document. It resolves each citation to its source text and reports those sharing no vocabulary for human review; empirically 10–18% are correct-but-differently-worded, so it screens rather than decides. | Root-cause response to the defect recorded in entry 053. | Documentation Manager (AI-assisted) | Not Submitted |
| 056 | 2026-08-17 | **CMP-DOC-10** | **API Specification** | **0.1** | **Initial issue.** Created `DOC-10-API-CMP-API-Specification.md` (7 Mermaid diagrams) and `DOC-10-API-CMP-API-Specification.docx` (cover page, live TOC field, header/footer with page numbering, 94 captioned tables, 7 rendered diagrams). Establishes **10 interface drivers** and **14 API Decision Records**, and issues **216 statements** (`API-001`…`API-216`) covering the interface model, versioning and negotiation, representation rules, idempotency, the four-branch error model, authorisation and session carriage, collections, the resource catalogue, the safety surface, the provider callback surface, configuration delivery, rate limiting and interface quality obligations. **All 3 obligations from CMP-DOC-08 §17.5 and the 1 from CMP-DOC-09 §18.5 are discharged**, and **16 of 16 SAD interface statements are realised**. **100 of 216 statements are integrity-critical — 46%, the highest proportion in the chain**, because the interface is where every absolute rule is either enforced or lost. **No page size, rate, window, limit or latency figure is invented** — 9 tuning values are named as configuration. **No resource is invented for undecided behaviour** — 11 are named as withheld with the decision blocking each. Records 2 statements with no upstream counterpart (both inherited from CMP-DOC-09 §18.7), 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create API Specification"**. | Solution Architect / Backend Lead (AI-assisted) | **Not Submitted — Draft for review** |
| 057 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.2 | DOC-10 set to v0.1 Draft; `API-001`…`API-216` recorded as issued; Index §5.10 registers the 5 auxiliary prefixes; totals updated to 1,705 traceable identifiers and 1,470 statements; programme metrics to 10 Not Started / 10 Draft / 50% drafted; 8 API metrics added to §7.2; 6 interface terms added to the Glossary; RTM records 216 `API` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-10. | Documentation Manager (AI-assisted) | Not Submitted |
| 058 | 2026-08-17 | **CMP-DOC-11** | **Database Design Document** | **0.1** | **Initial issue.** Created `DOC-11-DATABASE-CMP-Database-Design.md` (6 Mermaid diagrams) and `DOC-11-DATABASE-CMP-Database-Design.docx` (cover page, live TOC field, header/footer with page numbering, 87 captioned tables, 6 rendered diagrams). Establishes **10 data drivers** and **16 Database Decision Records**, and issues **232 statements** (`DB-001`…`DB-232`) covering six storage domains, schema conventions, the operational schema across 34 tables, the seat allocation record, the ledger, the evidential log, projections, operational machinery, historical integrity, retention and removal, indexing and access paths, the integrity constraint register, migration, and backup and recovery. **All four obligations from CMP-DOC-09 §18.5 and CMP-DOC-10 §17.7 are discharged**, **all 20 Persistence requirements are realised** (4 as mechanism without a value), and **7 of 10 absolute rules are enforced by the database itself** — the other 3 being behavioural and named as such. Specifies **21 database-enforced constraints** in one reviewable register, each with a required test. **No retention period, RPO, RTO, volume or sizing figure is invented** — 8 of 9 retention periods and all 7 sizing decisions are named as unset. **No table is created for undecided behaviour** — 7 are withheld with the decision blocking each. Records 3 statements with no upstream counterpart, of which `DB-230` (a restore must not reinstate removed personal data) is **new here and was not considered by any predecessor**. 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create Database Design"**. | Software Architect / Backend Lead (AI-assisted) | **Not Submitted — Draft for review** |
| 059 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.3 | DOC-11 set to v0.1 Draft; `DB-001`…`DB-232` recorded as issued; Index §5.11 registers the 5 auxiliary prefixes; totals updated to 1,937 traceable identifiers and 1,518 statements; programme metrics to 9 Not Started / 11 Draft / 55% drafted; 10 database metrics added to §7.2; 7 database terms added to the Glossary; RTM records 232 `DB` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-11. | Documentation Manager (AI-assisted) | Not Submitted |
| 060 | 2026-08-17 | **CMP-DOC-13** | **Security Design** | **0.1** | **Initial issue, produced out of sequence** per CMP-DOC-07 `AR-09`. Created `DOC-13-SECURITY-CMP-Security-Design.md` (5 Mermaid diagrams) and `DOC-13-SECURITY-CMP-Security-Design.docx` (cover page, live TOC field, header/footer with page numbering, 88 captioned tables, 5 rendered diagrams). Establishes **10 security drivers** and **16 Security Decision Records**, and issues **240 statements** (`SEC-001`…`SEC-240`) covering trust boundary defences, authentication, session management, authorisation and disclosure, protection at rest and in transit, the evidential chain mechanism, injection defence, payment credential handling, client-side security, secrets and key management, abuse posture, the fraud position, security logging, backup security and verification. **All 18 security quality requirements are mechanised** — 16 verifiably, 2 named as not. **Three of the four `AR-09` unmechanised properties are closed**; fraud is not, and §16 states why. **Both requirement-chain gaps are adopted and closed** — payment credential handling (strengthened from "never stored" to "never received") and injection defence — closing `BE-OQ-08`, `BE-OQ-09`, `API-OQ-05` and `DB-OQ-08` together. Specifies **14 automated checks**, 7 non-suppressible, and records **3 properties as unverifiable** rather than presenting them as satisfied. **No compliance, certification or regulatory claim is made anywhere**, and no rate limit, key rotation period, breach notification timing or fraud rule is invented. States explicitly in three places what the design does **not** defend against. Records 4 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions, **5 of which are the Project Owner's alone**. | Explicit Project Owner command: **"Create Security Design"**, following CMP-DOC-07 `AR-09` and three separate obligations from CMP-DOC-11 §18.7. | Security Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 061 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.4 | DOC-13 set to v0.1 Draft; `SEC-001`…`SEC-240` recorded as issued; Index §5.12 registers the 5 auxiliary prefixes; totals updated to 2,177 traceable identifiers and 1,566 statements; programme metrics to 8 Not Started / 12 Draft / 60% drafted; 10 security metrics added to §7.2; **constraint 6 (the two requirement-chain gaps) marked CLOSED**; 6 security terms added to the Glossary; RTM records 240 `SEC` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-13. | Documentation Manager (AI-assisted) | Not Submitted |
| 062 | 2026-08-17 | **CMP-DOC-12** | **UI/UX Specification** | **0.1** | **Initial issue.** Created `DOC-12-UIUX-CMP-UI-UX-Specification.md` (5 Mermaid diagrams) and `DOC-12-UIUX-CMP-UI-UX-Specification.docx` (cover page, live TOC field, header/footer with page numbering, 82 captioned tables, 5 rendered diagrams). Establishes **10 experience drivers** and **16 UI/UX Decision Records**, and issues **224 statements** (`UX-001`…`UX-224`) covering the screen inventory, the on-screen state model, provenance treatment, the four failure treatments, navigation, the commitment surface, search and results, the payment handoff, the trip surface, the safety surface, content and refusal reasons, accessibility mechanics, offline behaviour and the screens withheld. **All five obligations from CMP-DOC-08 §17.5 and CMP-DOC-10 §17.7 are discharged.** Specifies **32 screens**, marks **4 as partial**, and **withholds 14** with the use case and decision blocking each. **No colour, typeface, logo or brand element is specified** (brand is TBD) and **no accessibility conformance is claimed** (standard unchosen). Two decisions carry unusual weight: `UXDR-06` makes the commit control unavailable while any of the six `NFR-084` values is cached — blocking booking on a poor connection, deliberately — and `UXDR-13` designs **no SOS control**. Records 3 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create UI/UX Specification"**. | UI/UX Designer (AI-assisted) | **Not Submitted — Draft for review** |
| 063 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.5 | DOC-12 set to v0.1 Draft; **`UX-` prefix allocated and registered** (Index §5.13, conflict `CC-014`); `UX-001`…`UX-224` recorded as issued; totals updated to 2,401 traceable identifiers and 1,614 statements; programme metrics to 7 Not Started / 13 Draft / 65% drafted; 10 UI/UX metrics added to §7.2; 5 experience terms added to the Glossary; RTM records 224 `UX` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-12. | Documentation Manager (AI-assisted) | Not Submitted |
| 064 | 2026-08-17 | **CMP-DOC-14** | **Payment & UPI Specification** | **0.1** | **Initial issue.** Created `DOC-14-PAYMENT-CMP-Payment-UPI-Specification.md` (5 Mermaid diagrams) and `DOC-14-PAYMENT-CMP-Payment-UPI-Specification.docx` (cover page, live TOC field, header/footer with page numbering, 85 captioned tables, 5 rendered diagrams). Establishes **10 payment drivers** and **14 Payment Decision Records**, and issues **208 statements** (`PAY-001`…`PAY-208`) covering the money model, fare establishment, the UPI handoff, verification, payment states, the provider port, the callback surface, reconciliation, the ledger, degraded operation, provider selection criteria, what is not specified, and verification obligations. **All 24 payment functional requirements are realised** and **all three zero-tolerance quality requirements (`NFR-027`, `NFR-029`, `NFR-032`) are achieved by construction rather than by discipline.** All five inherited obligations from CMP-DOC-10, 12 and 13 are discharged, including `SEC-ASM-04` verification, which is made a **provider selection gate rather than an integration discovery**. **No provider is named** (`BE-161`; three of five unchosen) and **no scheme or regulatory position is asserted**. Two decisions carry unusual weight: `PADR-03` **discards** the UPI application's response rather than distrusting it, and `PADR-06` gives `pending` **no timeout**, because every timeout is an assumption with a clock attached. **§15 records six unresolved money decisions and invents none of them** — the receipt-and-verification half is fully specified and the money-out half does not exist. Records 4 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions, **six of which are the Project Owner's alone**. | Explicit Project Owner command: **"Create Payment & UPI Specification"**. | Solution Architect / Payments (AI-assisted) | **Not Submitted — Draft for review** |
| 065 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.6 | DOC-14 set to v0.1 Draft; `PAY-001`…`PAY-208` recorded as issued; Index §5.14 registers the 5 auxiliary prefixes; totals updated to 2,609 traceable identifiers and 1,662 statements; programme metrics to 6 Not Started / 14 Draft / 70% drafted; 9 payment metrics added to §7.2; **constraint 4 rewritten to record the six unresolved money decisions**; 6 payment terms added to the Glossary; RTM records 208 `PAY` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-14. | Documentation Manager (AI-assisted) | Not Submitted |
| 066 | 2026-08-17 | **CMP-DOC-15** | **GPS / Live Trip Specification** | **0.1** | **Initial issue.** Created `DOC-15-GPS-CMP-Live-Trip-Specification.md` (4 Mermaid diagrams) and `DOC-15-GPS-CMP-Live-Trip-Specification.docx` (cover page, live TOC field, header/footer with page numbering, 83 captioned tables, 4 rendered diagrams). Establishes **10 tracking drivers** and **14 Tracking Decision Records**, and issues **196 statements** (`GPS-001`…`GPS-196`) covering the trip lifecycle, position acquisition, position reporting, staleness, estimates, the multi-passenger trip, position privacy and retention, mapping dependency and cost, what is not specified, degraded operation, verification obligations and configuration values. **27 of 28 trip execution requirements are realised**; `FRD-FR-160` is Release R2. All five inherited obligations discharged, including **CMP-DOC-12 §18.7's staleness bound**, now supplied as configuration. Two decisions carry unusual weight: `GADR-03` carries **two instants** per position — the platform's authoritative receipt instant and the device's non-authoritative acquisition instant, because the age a user needs and the value the platform can trust are different — and `GADR-07` **never extrapolates**, which makes the map flicker honestly rather than lie smoothly. **No cadence, staleness bound, accuracy, battery, data or cost figure is stated** — 11 values are named unset — and **no trip state name is invented**, `UC-041` being Outlined. **A new gap was identified: `GAP-017`, pickup and drop sequencing on a multi-passenger trip**, which no upstream rule, decision, requirement or gap entry covers and without which `FRD-FR-159` is unimplementable. Records 5 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create GPS / Live Trip Specification"**. | Software Architect (AI-assisted) | **Not Submitted — Draft for review** |
| 067 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.7 | DOC-15 set to v0.1 Draft; `GPS-001`…`GPS-196` recorded as issued; Index §5.15 registers the 5 auxiliary prefixes; totals updated to 2,805 traceable identifiers and 1,710 statements; programme metrics to 5 Not Started / 15 Draft / 75% drafted; 7 tracking metrics added to §7.2; **`GAP-017` added to the gap register**; 5 tracking terms added to the Glossary; RTM records 196 `GPS` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-15. | Documentation Manager (AI-assisted) | Not Submitted |
| 068 | 2026-08-17 | **CMP-DOC-16** | **Communication & Notification Specification** | **0.1** | **Initial issue.** Created `DOC-16-NOTIFICATION-CMP-Communication-Notifications.md` (4 Mermaid diagrams) and `DOC-16-NOTIFICATION-CMP-Communication-Notifications.docx` (cover page, live TOC field, header/footer with page numbering, 73 captioned tables, 4 rendered diagrams). Establishes **10 communication drivers** and **14 Communication Decision Records**, and issues **188 statements** (`NOTIF-001`…`NOTIF-188`) covering the messaging model, message delivery, the notification model, categories and preferences, delivery and failure, the delivery port, notification content, privacy and protection, what is not specified, verification obligations and configuration values. **All 24 communication and notification requirements are realised**, and **all four inherited obligations from CMP-DOC-12 §18.7 and CMP-DOC-15 §16.7 are discharged**. Two decisions carry unusual weight: `NADR-05` allows a notification to carry **no business value at all**, because it is the one surface read outside the application with no provenance and no entitlement check; and `NADR-07` places mandatory categories **absent from the preference surface** rather than shown disabled, because a visible control is an offer and `FRD-FR-202` forbids offering one. **No disclosure rule between users is invented** — `BAD-DEC-022` leaves four messaging requirements realised as mechanism without a boundary rule, and §12.1 states that the conversation header cannot be built from this document. **No second delivery channel, reminder, digest, moderation or read receipt is invented.** Records 4 statements with no upstream counterpart, including **`NOTIF-176`**: `NFR-076` guarantees a safety notification is not suppressed by preference and guarantees nothing about it reaching a device that is off, which with one channel is the weakest point in the safety path. 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create Communication & Notification Specification"**. | Software Architect (AI-assisted) | **Not Submitted — Draft for review** |
| 069 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.8 | DOC-16 set to v0.1 Draft; `NOTIF-001`…`NOTIF-188` recorded as issued; Index §5.16 registers the 5 auxiliary prefixes; totals updated to 2,993 traceable identifiers and 1,758 statements; programme metrics to 4 Not Started / 16 Draft / 80% drafted; 8 communication metrics added to §7.2; 5 communication terms added to the Glossary; RTM records 188 `NOTIF` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-16. | Documentation Manager (AI-assisted) | Not Submitted |
| 070 | 2026-08-17 | **CMP-DOC-17** | **Admin / Filament Specification** | **0.1** | **Initial issue.** Created `DOC-17-ADMIN-CMP-Filament-Specification.md` (3 Mermaid diagrams) and `DOC-17-ADMIN-CMP-Filament-Specification.docx` (cover page, live TOC field, header/footer with page numbering, 76 captioned tables, 3 rendered diagrams). Establishes **10 administrative drivers** and **14 Administrative Decision Records**, and issues **204 statements** (`ADM-001`…`ADM-204`) covering the binding constraint, resource structure, the projection inventory, inspection, intervention, payment reconciliation, safety response, support cases, operational reporting, roles and authorisation, evidencing, screens withheld and verification obligations. **All 28 administration requirements and all 14 Administrative Application requirements are realised**, and **all seven obligations from five predecessors are discharged** — more inherited obligations than any other document in the chain. **`DB-139` is closed**: the projection inventory is settled at nine. The governing decision is `ADMR-01` / `ADM-002` — **no Filament resource declares an Eloquent model** — which removes the mechanism of `SRS-RISK-003`, the highest-rated integrity risk in the requirements chain, at the deliberate cost of substantially forfeiting Filament's productivity advantage. §17.4 records `TB-2` moving from *partially defended* to defended. **No role is named** (`SEC-063`), and `ADM-168` records that the surface **cannot be deployed until at least one role exists**. **Five of twelve operator capabilities are withheld**, two of them operationally serious: no operator can adjudicate a verification submission (`ADM-195`) and no operator can change an account state (`ADM-196`), so a safety responder can close an incident and take no action against the account involved. Records 4 statements with no upstream counterpart, 6 assumptions, 8 risks — including `ADM-RISK-03` at **severity 25, the highest single risk in the chain** — and 8 open questions. | Explicit Project Owner command: **"Create Admin / Filament Specification"**. | Backend Lead / Product Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 071 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 1.9 | DOC-17 set to v0.1 Draft; `ADM-001`…`ADM-204` recorded as issued; Index §5.17 registers the 5 auxiliary prefixes; totals updated to 3,197 traceable identifiers and 1,806 statements; programme metrics to 3 Not Started / 17 Draft / 85% drafted; 10 administrative metrics added to §7.2; **constraints 8 and 9 added** (role set blocks deployment; five operator capabilities withheld); 5 administrative terms added to the Glossary; RTM records 204 `ADM` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-17. | Documentation Manager (AI-assisted) | Not Submitted |
| 072 | 2026-08-17 | **CMP-DOC-18** | **Testing & QA Documentation** | **0.1** | **Initial issue.** Created `DOC-18-TESTING-CMP-Testing-QA.md` (2 Mermaid diagrams) and `DOC-18-TESTING-CMP-Testing-QA.docx` (cover page, live TOC field, header/footer with page numbering, 87 captioned tables, 2 rendered diagrams). Establishes **10 quality drivers** and **14 Testing Decision Records**, and issues **216 statements** (`TC-001`…`TC-216`) covering the consolidated obligation register, verification levels, static analysis and build gates, unit and domain testing, integration and contract testing, database constraint testing, security verification, client testing, concurrency and failure injection, accessibility verification, what cannot be tested, coverage and reporting, and test data. **Consolidates 99 verification obligations from nine documents into one register**, identifies the **25 non-suppressible** in one place for the first time, and consolidates the structural rules into **13**. **All thirteen obligations placed directly on this document are discharged.** The principal finding is `TC-014`: **99 obligations do not cover the 1,164 integrity-critical statements the chain has produced** — nine authors each protected what they could see and nobody could see the union — and `TC-020` forbids reporting a coverage figure until the statement-by-statement mapping (`TC-017`) is complete. `TADR-11` establishes that **measuring is not verifying**: five verification categories have no target to fail against and are reported as measurement, never as passes. `TADR-13` specifies **no test for a withheld capability**, and `TC-181` states plainly that a fully passing suite leaves the largest known product risks untouched. **No coverage figure, threshold, target or tool name appears anywhere.** Records 4 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create Testing & QA Documentation"**. | QA Analyst (AI-assisted) | **Not Submitted — Draft for review** |
| 073 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 2.0 | DOC-18 set to v0.1 Draft; `TC-001`…`TC-216` recorded as issued; Index §5.18 registers the 5 auxiliary prefixes and notes that test cases are identified by obligation reference; totals updated to 3,413 traceable identifiers and 1,854 statements; programme metrics to 2 Not Started / 18 Draft / 90% drafted; 10 verification metrics added to §7.2 including the chain-wide ‡ count; **constraint 10 added** (99 obligations against 1,164 ‡ statements, mapping outstanding); 6 verification terms added to the Glossary; RTM records 216 `TC` defined with 0 traced forward. **Control files reach v2.0.** | Workflow Steps 12–16 following creation of CMP-DOC-18. | Documentation Manager (AI-assisted) | Not Submitted |
| 074 | 2026-08-17 | **CMP-DOC-19** | **DevOps / Deployment Documentation** | **0.1** | **Initial issue.** Created `DOC-19-DEVOPS-CMP-Deployment.md` (3 Mermaid diagrams) and `DOC-19-DEVOPS-CMP-Deployment.docx` (cover page, live TOC field, header/footer with page numbering, 107 captioned tables, 3 rendered diagrams). Establishes **10 deployment drivers** and **14 Deployment Decision Records**, and issues **208 statements** (`OPS-001`…`OPS-208`) covering the consolidated sizing register, environments and promotion, deployment topology and units, configuration, secrets and credential custody, the build and release pipeline, client build and distribution, database operations, observability and alerting, availability and the safety exception, incident response, what cannot be decided, and provisioning without a provider. **All 16 obligations placed on this document by nine predecessors are addressed** — 14 in full, 2 (restore reconciliation and incident response) structurally only, with the limit stated. **Consolidates 21 sizing decisions** — 11 from CMP-DOC-07 §11.2, 7 from CMP-DOC-11 §14.5, 3 arising here — **none resolved, none estimated**. The principal finding is `OPS-014`: **this document specifies the properties of a deployment and cannot specify a deployment**, because deployment is the first place in the chain where structure and quantity stop being separable. `DDR-01` establishes **properties, never products** — zero providers, services or tools are named, since `BAD-DEP-009` leaves hosting unselected. `DDR-09` realises the safety exception across maintenance, routing, capacity and deployment order, and `OPS-169` (new) requires the safety unit to deploy first on a shared-code change. `OPS-189` records that **both owed procedures are complete in structure and empty in content**. **No capacity, threshold, sizing or timing figure appears anywhere.** Records 4 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create DevOps / Deployment Documentation"**. | DevOps Engineer (AI-assisted) | **Not Submitted — Draft for review** |
| 075 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 2.1 | DOC-19 set to v0.1 Draft; `OPS-001`…`OPS-208` recorded as issued; **`OPS-` registered in the namespace allocation table** with `CC-021` noting that no prefix was allocated by §20 and that `DEP-` was rejected for collision with `BAD-DEP-`; Index §5.19 registers the 5 auxiliary prefixes; totals updated to 3,621 traceable identifiers and 1,900 statements; programme metrics to 1 Not Started / 19 Draft / 95% drafted; 10 deployment metrics added to §7.2; **constraints 11 and 12 added** (21 sizing decisions with no figure; two owed procedures that cannot be completed); 8 deployment terms added to the Glossary; RTM records 208 `OPS` defined with 0 traced forward. | Workflow Steps 12–16 following creation of CMP-DOC-19. | Documentation Manager (AI-assisted) | Not Submitted |
| 076 | 2026-08-17 | **CMP-DOC-20** | **Requirements Traceability & Release Documentation** | **0.1** | **Initial issue. The final document in the chain.** Created `DOC-20-TRACEABILITY-CMP-Traceability-Release.md` (3 Mermaid diagrams) and `DOC-20-TRACEABILITY-CMP-Traceability-Release.docx` (cover page, live TOC field, header/footer with page numbering, 123 captioned tables, 3 rendered diagrams). Establishes **10 traceability drivers** and **14 Traceability Decision Records**, and issues **192 statements** (`TR-001`…`TR-192`) covering the chain measured, forward traceability, the thirty-seven, backward traceability, the consolidated open-decision register, the obligation ledger, release readiness criteria, current status, the release report, baseline and change control, what cannot be reported, and chain completion. **All three obligations placed on this document are discharged.** Measured the whole chain from source: **3,621 traceable statements, 1,412 integrity-critical, across 19 documents**. The principal finding is `TR-034`: **the chain anchors design statements to business rules rather than to the requirements derived from them, so 208 of 955 upstream requirements carry no downstream citation — 37 of them integrity-critical.** Spot resolution of three shows they are realised under other citations, which makes it a defect in the traceability rather than necessarily in the platform; `TR-032` records that the two cannot be distinguished without statement-by-statement review. Backward traceability is complete (0 unresolved citations); forward traceability is not, and `TR-067` records that the asymmetry is structural. Consolidates **128 open decisions and withheld items in 6 classes** and **84 obligations** into two registers. Specifies **12 release readiness criteria of which 1 is met**, and a **9-category release report** that may not be collapsed into a single figure. `TRDR-06` creates **no traceability link**; `TRDR-10` states **no release date, estimate or readiness percentage**; `TRDR-03` forbids reporting citation coverage as verification coverage. Records 4 statements with no upstream counterpart, 6 assumptions, 8 risks and 8 open questions. | Explicit Project Owner command: **"Create Requirements Traceability & Release Documentation"**. | Documentation Manager (AI-assisted) | **Not Submitted — Draft for review** |
| 077 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 3.0 | DOC-20 set to v0.1 Draft; `TR-001`…`TR-192` recorded as issued; **`TR-` registered in the namespace allocation table** with `CC-023`; Index §5.20 registers the 5 auxiliary prefixes and records that **the chain is complete**; totals updated to 3,813 traceable identifiers and 1,992 statements; programme metrics to **0 Not Started / 20 Draft / 100% drafted**; 14 traceability metrics added to §7.2 including the chain-wide measurement and the 37; **constraints 13 and 14 added** (208 uncited requirements with 37 integrity-critical; 1 of 12 release criteria met); 8 traceability terms added to the Glossary; RTM records 192 `TR` defined with no successor. **Control files reach v3.0 and the documentation chain is complete at 20 of 20 documents, none approved.** | Workflow Steps 12–16 following creation of CMP-DOC-20. | Documentation Manager (AI-assisted) | Not Submitted |
| 078 | 2026-08-17 | **CMP-DOC-20A** | **Review of the 37 Uncited Integrity-Critical Requirements** | **0.1** | **Initial issue. Annex A to CMP-DOC-20.** Created `DOC-20A-TRACEABILITY-CMP-Uncited-Requirement-Review.md` and `.docx` (17 captioned tables). Discharges `TR-042` and `TR-048`: **all 37 reviewed individually, none closed by assertion, no traceability link created**. **72 realisers were named and all 72 were resolved to their source text and verified; none was written from recollection.** Outcomes: **6 already cited** in CMP-DOC-04's abbreviated business-requirement column (a fifth outcome the parent document did not anticipate); **27 realised, cited elsewhere**, needing 72 citations across 55 statements in 11 documents; **2 realised with no statement identifiable** (`FRD-FR-083`, `SRS-REQ-128`); **1 not realised** (`NFR-138`); **1 superseded** (`FRD-FR-243`). The material finding is that **`NFR-138` — recording each user's agreement to the rules of participation, with the version agreed — is realised nowhere in the chain**, though `BRD-CMP-008` requires it. Proposes and registers `GAP-018`, `GAP-019` and `GAP-020`. Records 7 actions awaiting direction; **none has been performed**, because `TRDR-06` reserves the changes to the documents that own them. | Explicit Project Owner command: **"Review the 37 uncited integrity-critical requirements"**. | Documentation Manager (AI-assisted) | **Not Submitted — Draft for review** |
| 079 | 2026-08-17 | **CMP-DOC-20** | **Requirements Traceability & Release Documentation** | **0.1 → 0.2** | **Correction following CMP-DOC-20A.** The forward measure at v0.1 counted only full `BRD-REQ-nnn` identifiers and did not read **CMP-DOC-04's abbreviated business-requirement column**, in which the linkage is recorded as bare three-digit numbers. 254 FRD rows reference 107 distinct business requirements that way. Corrected: **uncited 208 → 177**; **uncited integrity-critical 37 → 31**; uncited `BRD-REQ` 64 → 33 and its integrity-critical count **6 → 0**; Appendix C.2 regenerated at **146** entries. `TR-025`, `TR-026`, `TR-027`, `TR-029`, `TR-050`, `TR-167` and `TR-175` amended; **outcome 0 added to §6.3**, a **sixth limit** to §14.1 and a **fifth disagreement** to §14.3 — this document's own. §19.2 rewritten to report the completed review. **`TR-034` stands**: the chain still anchors to rules rather than requirements, and the correction changes the size of the finding, not the finding. | The review recorded in CMP-DOC-20A found this document's measure partly wrong; `TRDR-01` and `TRDR-14` require correction by re-measurement. | Documentation Manager (AI-assisted) | Not Submitted |
| 080 | 2026-08-17 | CMP-CTRL-INDEX / STATUS / GLOSSARY / RTM | Control files | 3.1 | CMP-DOC-20A registered as an annex issuing no identifiers; CMP-DOC-20 recorded at v0.2; **`GAP-018`, `GAP-019` and `GAP-020` registered** in the Master Traceability Matrix, taking the gap register to **20**; §7.2 traceability metrics corrected to 177 / 31 with the review outcome added; **programme constraint 13 rewritten and constraint 15 added** for `GAP-018`; three terms added to the Glossary. | Workflow Steps 12–16 following CMP-DOC-20A and the correction of CMP-DOC-20. | Documentation Manager (AI-assisted) | Not Submitted |
| 081 | 2026-08-19 | **CMP-PLAN-IMPL-TRACKER** | **Implementation Work Register** | **0.1** | **Initial issue. Planning artefact, outside the specification chain.** Created `CMP-Implementation-Tracker.xlsx` (13 sheets: dashboard, work-item register, feature summary, daily progress, commits, pushes, testing, bugs, decisions, dependencies, risks, document traceability, drop-downs) and `CMP-Implementation-Tracker.docx` (printable rendering generated from the workbook). **507 implementation work items across 39 features, 3,802 estimated hours, all at `Not Started` (496) or `Blocked` (11) at issue** — nothing is marked implemented, committed, pushed, tested, reviewed or completed. Issues `CMP-IMP-` and `FEAT-`, registered in Documentation_Index §5.21; **neither participates in the forward traceability chain**. **562 work IDs issued, 55 withdrawn** as not-implementation-work and never reissued. **No business decision was invented, guessed at or given a provisional value; no functional gap, withheld area, risk or documentation conflict was converted into a work item** — all are carried on the decision, dependency and risk registers instead. | Project Owner request for an implementation work breakdown and progress tracker derived from CMP-DOC-01 … CMP-DOC-20. | Technical Project Management (AI-assisted) | Not Submitted |
| 082 | 2026-08-19 | **CMP-PLAN-IMPL-ANALYSIS** | **Implementation Analysis** | **0.1** | **Initial issue. Planning artefact, outside the specification chain.** Created `CMP-Implementation-Analysis.md` and `.docx` (24 sections). Reports what the decomposition found: **no application source code exists**, so no functionality is partially implemented and **no mock or placeholder work exists**; the 15-day window carries **336 items and 2,540 hours**, which is dependency-ordered rather than capacity-fitted and implies a team roughly four times larger than a small one; a recommended MVP cut line and the items that cannot realistically fit; the 55 withdrawn work IDs itemised at §24. **Records one documentation conflict discovered during decomposition** — see `CC-025`. | Companion analysis required by the same Project Owner request as entry 081. | Technical Project Management (AI-assisted) | Not Submitted |
| 083 | 2026-08-19 | CMP-CTRL-INDEX / CHANGELOG | Control files | 3.2 | **Documentation Index §3A added** registering the two planning artefacts outside the formal chain, with §3A.1 recording the register contents; **§5.21 added** registering the `CMP-IMP-` and `FEAT-` prefixes and the 55 withdrawn identifiers; repository totals annotated as unchanged, because planning identifiers are not chain identifiers; maintenance rules 5 and 6 added. Section 3A is **lettered, not numbered**, so that existing cross-references to §4, §5 and §6 — including `CC-021` and `CC-023` — remain valid. Change Log entries 081–083 and conflicts `CC-024`, `CC-025` recorded. | Workflow Steps 12–16 following the issue of the implementation planning artefacts. | Documentation Manager (AI-assisted) | Not Submitted |
| 084 | 2026-08-19 | CMP-CTRL-STATUS / INDEX / CHANGELOG | Control files | 3.3 | **Documentation Status §5A added**, parallel to §5, tracking the lifecycle status of the two planning artefacts; **§5A.1 records the three lifecycle rules that do not carry over** from the formal chain (no baselining position, approval means the breakdown is accepted rather than any work being complete, and a chain revision makes the artefacts **stale** rather than obliging a reissue); **§5A.2 records the current position and a staleness marker**; §3 gains separate planning-artefact counters, kept **out of** the 3,813 traceable-ID total; §4 control-file versions updated; maintenance rules 5 and 6 added. **CMP-CTRL-STATUS moves 3.1 to 3.3**, skipping 3.2, so that all three revised control files carry the same version; Glossary and RTM remain at 3.1, untouched. **Documentation Index §2 corrected** — the control-file version column had been stale at 0.9 for every entry since v0.9, contradicting the same files’ own front matter. **Documentation Index §3A corrected** — it previously stated that planning artefact versions were not tracked in Documentation_Status.md, which ceased to be true with this entry. Section 5A is **lettered, not numbered**, so that existing cross-references to §6, §7 and §8 remain valid. | Project Owner request to register the planning artefacts in the status file as well as the index. | Documentation Manager (AI-assisted) | Not Submitted |
| 085 | 2026-08-19 | CMP-CTRL-README / INDEX / STATUS / CHANGELOG | Control files | README 0.2; others 3.4 | **Repository root relocated on Project Owner instruction.** The project root is now `carpool-mobility-platform`, matching the GitHub repository name; the former root `bikeride-app` retains nothing but the new root. The entire `Document/` tree (52 files, 21 subdirectories) plus `CLAUDE.md` and `CLAUDE-READINESS-ANALYSIS.md` were relocated intact. **Verified by SHA256 manifest taken before the move: 54 of 54 files byte-identical at the new location, 0 mismatches, 0 missing**; the implementation tracker workbook was re-opened and confirmed at 13 sheets and 507 work items. **No document content changed as a consequence of the move** — every path recorded in Documentation_Index and Documentation_Status is relative and begins with `Document/`, so all resolve unchanged against the new root and no path correction was required. Two stale references to the former root name were corrected on instruction: **README §2 NAMING RULE** (now naming `carpool-mobility-platform`, restating that a folder or repository name is not a brand decision, and that `BAD-DEC-023` remains open) and **CLAUDE.md §1** (same position, extended to cover names used informally in conversation). **README §12 was deliberately not altered**: its `FACT` statement records the repository as it stood at initialisation on 2026-08-16 and is historically accurate; rewriting it would falsify the record. Control-file version tables in Index §2 and Status §4 updated for consistency. | Project Owner instruction to consolidate the project under a single root matching the GitHub repository, then to correct the two stale root-name references. | Documentation Manager (AI-assisted) | Not Submitted |
| 086 | 2026-08-19 | CMP-CTRL-README / INDEX / STATUS / CHANGELOG | Control files | README 0.3; others 3.5 | **README §12 Current Repository State corrected.** The section had been stale since the second document was written: it recorded the twenty formal documents as **Not Started** when all twenty exist, and named the next expected action as `Create BAD` (DOC-01), which was completed on 2026-08-16. Restated to record the current root, the created directory structure including the empty `backend/` and `Mobile/`, all twenty documents created and **none approved**, the planning artefacts, the absence of any application source code, that **version control is not initialised and no Git policy exists**, and that the development gate is **NO**. A dated heading and a pointer to `Document_Change_Log.md` were added so the section is understood as a restated snapshot rather than a running history. **The 2026-08-16 initialisation FACT is retained verbatim and labelled historical**, with a second dated FACT recording the relocation; neither restates the other. No requirement, rule or governance statement was altered, and no status was advanced. | Project Owner instruction to correct §12 following the root relocation at entry 085. | Documentation Manager (AI-assisted) | Not Submitted |
| 087 | 2026-08-19 | CMP-CTRL-README / INDEX / STATUS / CHANGELOG | Control files | README 0.4; others 3.6 | **Technical decisions ratified by the Project Owner; implementation began.** `TECHNICAL-DECISIONS.md` was created at project root recording `TECH-DEC-001` … `TECH-DEC-005` — the six technical blockers `CLAUDE-READINESS-ANALYSIS.md` §21.1 calls "stop the first commit" (B1–B6) and §22.1 lists as T1–T4 plus the environment variable inventory. The Project Owner **ratified all five exactly as recorded**, and directed that they not be modified, reinterpreted or extended. They are: **`TECH-DEC-001`** runtime and framework versions — PHP 8.3, Laravel 13, MySQL 8.4 LTS (`OPS-024` ‡ requires a server that enforces `CHECK`; MySQL does so from 8.0.16), Filament and the Android toolchain **deliberately not decided**; **`TECH-DEC-002`** Git and GitHub policy — trunk `main`, `feature/CMP-IMP-nnn-*` branches merged `--no-ff`, Conventional Commits naming the work item, no tag or release scheme, UI work not committed before the human visual test gate; **`TECH-DEC-003`** static analysis and lint tooling — Deptrac 4, PHPStan 2 at level 9 with Larastan 3, Laravel Pint, and a PHPUnit `Architecture` suite, **with no baseline file and no suppression list in any configuration** (`TC-042`); **`TECH-DEC-004`** test frameworks — PHPUnit 12 across five suites named for the six verification levels of `TC-025`, so that `TC-028` is a property of the runner; **`TECH-DEC-005`** the environment variable inventory — `backend/.env.example`, carrying names and non-secret defaults only (`BE-015`, `SADR-14`, `OPS-098`). **`T5` monetary precision (`DB-032`, `GAP-016`) and `T6` route-overlap algorithm and threshold (`ARCH-OQ-001`) were expressly excluded from the ratification and remain open**; the Project Owner directed that neither be decided. `TECHNICAL-DECISIONS.md` is a **record of technical decisions taken under delegated authority; it is not a specification document, creates no requirement, takes no business decision and closes no gap**, and it issues no identifier into the forward traceability chain. It is held at project root alongside `CLAUDE.md` and `CLAUDE-READINESS-ANALYSIS.md`, which are likewise not registered in `Documentation_Index.md`. `README.md` §12 Current Repository State restated: application source code now exists in `backend/`, version control is initialised, and the **development gate is open for the backend** while the **deployment gate remains NO** (`CLAUDE-READINESS-ANALYSIS.md` §24) and every business blocker B7–B22 remains open. `Documentation_Status.md` §5A.2 restated for the same reason. Control-file version tables in Index §2 and Status §4 updated for consistency. **No formal document (CMP-DOC-01 … CMP-DOC-20) was created, modified or approved by this entry, and no work item status was changed** — the tracker's status columns are the Project Owner's. | Project Owner instruction of 2026-08-19: *"I ratify TECH-DEC-001 through TECH-DEC-005 exactly as recorded"*, and *"Record the ratification through the existing controlled-document/change-control process required by the project documentation."* | Documentation Manager (AI-assisted) | Not Submitted |
| 088 | 2026-08-20 | **CMP-DOC-13** | **Security Design** | **0.1 → 0.2** | **The evidential chain construction ratified and recorded in §14.2.** `SEC-107` required *"a keyed message authentication construction over a canonical serialisation of the record"* and left the specific algorithm to §14.2 as a technical decision; §14.2 named no algorithm, so the implementation of `FEAT-033` had to choose one to build the chain at all. The choice made and reported was **HMAC-SHA-256 over a length-prefixed canonical serialisation**, and the Project Owner ratified it on 2026-08-20. Two statements are issued into §14.2: **`SEC-241`** records the construction, citing `SEC-105`, `SEC-107` and `SEC-108`; **`SEC-242`** records that this does **not** close `SEC-174` — each record continues to carry the construction that produced it, so the algorithm remains replaceable by a staged migration under change control. The `SEC-174` design is preserved unchanged, as the Project Owner directed, and the `chain_algorithm` column that realises it is untouched. A **FACT (2026-08-20)** paragraph beneath the table records the ratification, restates that §0.6.2 continues to apply so that **no compliance or certification claim** is made, and records why a separator-joined serialisation was rejected: with a separator, an action of `a|b` with subject `c` and an action of `a` with subject `b|c` produce identical bytes, and two different records would share a hash — the length prefix is what makes `SEC-108` ‡ true in practice. The §14.2 construction table now cites `SEC-241` alongside `SEC-105` and `SEC-107`. Statistics updated: §1.1 and §23.1 to 242 statements (`SEC-001` … `SEC-242`), sources named 242 of 242, §14 from 16 statements to 18, total 242; §22 self-check items 9 and 10 restated, item 10 recording that §14 now holds `SEC-163`–`SEC-178` **and** `SEC-241`–`SEC-242` and that identifiers are never renumbered; §23.2 restated as 135 of 242 (still 56%); Appendix A given a `SEC-241` – `SEC-242` row. **No existing statement was altered, no ‡ marking was changed, the canonical serialisation, key handling and verification model are unchanged, the document remains Draft, and the `.docx` rendering is NOT regenerated — see `CC-028`.** | Project Owner instruction of 2026-08-20: *"I ratify the implementation choice of HMAC-SHA-256 over the length-prefixed canonical serialisation. Route this decision into CMP-DOC-13 §14.2 through the required controlled-document/change-control process. Preserve the existing `chain_algorithm` design so that the algorithm can be replaced in a controlled future migration. Do not weaken the canonical serialisation, key handling, or verification model."* | Security Analyst (AI-assisted) | Not Submitted |
| 089 | 2026-08-20 | **CMP-DOC-19** | **DevOps / Deployment Documentation** | **0.1 → 0.2** | **The server configuration requirement created by `DB-120` ‡ meeting `OPS-123` ‡ recorded in §11.1.** Building `FEAT-033` surfaced an interaction no document had confronted: `DB-120` ‡ requires a trigger on the evidential table as the second layer behind the withheld `UPDATE` and `DELETE` privilege, and `OPS-123` ‡ requires binary logging so that point-in-time recovery is possible. A server honouring both **refuses to create the trigger** unless it trusts the creating account or that account holds a privilege that overrides the check. The privilege route was attempted during implementation and **reverted**: `OPS-127` ‡ and `DADR-09` withhold evidential alteration from every account that does not need it, and `SUPER` or `SET_ANY_DEFINER` would hand the migration account far more than a one-off trigger creation requires, permanently, weakening the rule the trigger exists to reinforce. Two statements are issued into §11.1: **`OPS-209` ‡** requires the refusal to be resolved by **server configuration** and **never** by granting `SUPER`, `SET_ANY_DEFINER` or any broader privilege, citing `DB-120`, `OPS-123` and `OPS-127`; **`OPS-210`** requires the configuration satisfying `OPS-209` to be stated **per environment** and documented and reviewed before that environment passes its promotion gate, recording that a setting approved for one environment carries no approval for another and that **the production means is not stated here**. Explanatory prose records why `OPS-209` exists and why the privilege route was rejected, and a **FACT (2026-08-20)** records that the Project Owner approved `log_bin_trust_function_creators=ON` **for the development environment only**, applied as server configuration and recorded with that environment’s definition — **not approved for staging, pre-production or production**, and not a default. The FACT also records that this is the one place in the document where a concrete server configuration key is named, that it names a setting rather than a provider, service or product, and that `DDR-01` is unaffected; §19 self-check item 3 carries the same qualification. `OPS-210` is added to §17.5 as awaiting the production means (deployment review, `BAD-DEP-009`). Statistics updated: §1.1 and §20.1 to 210 statements (`OPS-001` … `OPS-210`), ‡ from 119 to 120, sources named 210 of 210, §11 from 18 statements to 20, total 210; §19 self-check items 11 and 12 restated, item 12 recording that §11 now holds `OPS-121`–`OPS-138` **and** `OPS-209`–`OPS-210`; Appendix A given an `OPS-209` – `OPS-210` row. **No existing statement was altered, no sizing figure, capacity, threshold or provider was introduced, no production deployment requirement was decided, the document remains Draft, and the `.docx` rendering is NOT regenerated — see `CC-028`.** | Project Owner instruction of 2026-08-20: *"I approve the use of `log_bin_trust_function_creators=ON` for the development Docker/MySQL environment as required to create the trigger under binary logging. Do not grant `SUPER`, `SET_ANY_DEFINER`, or any broader privilege to the migration account for this purpose. Record this requirement in CMP-DOC-19 through the project’s existing change-control process, including that it is a deployment/server configuration requirement and why it exists. Do not assume this setting is automatically appropriate for production. Production deployment requirements must remain explicitly documented and reviewed."* | DevOps Engineer (AI-assisted) | Not Submitted |
| 090 | 2026-08-20 | CMP-CTRL-INDEX / STATUS / CHANGELOG / RTM | Control files | Index, Status, Changelog 3.7; RTM 3.2 | **Control files brought into agreement with CMP-DOC-13 v0.2 and CMP-DOC-19 v0.2** (entries 088 and 089). Index §2 records the control-file versions and §3 records both documents at **0.2 Draft**; Index §5 namespace allocation now reads `SEC-001` … `SEC-242` (242 issued, v0.2) and `OPS-001` … `OPS-210` (210 issued, v0.2), each noting the section the new range was added to. Status §4, §5 and §7.2 updated to match, with §7.2 recording the two revised statement counts and the raised ‡ count. `Master_Traceability_Matrix.md` §4 identifier-integrity rows for both documents updated to v0.2 with their new ranges and counts, each recording that the section’s identifiers are **no longer one contiguous block** because identifiers are never renumbered (Index §6); the RTM metadata `Previous Version` field, which read `0.8 (2026-08-16)` and was stale, now reads `3.1 (2026-08-17)`. **A NOTE in both the RTM and Status §7.2 records that the chain totals — 3,621 statements and 1,412 ‡ — are CMP-DOC-20’s measurement at its own issue and are not recalculated here**; the chain now holds 3,625 statements and 1,413 ‡, and **CMP-DOC-20 is not revised by this change**. **`CC-028` opened**: the `.docx` renderings of CMP-DOC-13 and CMP-DOC-19 were created at v0.1 and are **not regenerated for v0.2**, so README §8.2 is not currently satisfied for those two documents. No project process authorises Claude Code to generate a `.docx`, so the regeneration is **recorded as outstanding for the Project Owner rather than performed**; the same position is restated for `CMP-Implementation-Tracker.docx`. Index §4 and Status §7.2 carry the outstanding item. **No formal document was created or approved by this entry, no document was marked Approved, and no work item status was changed.** | Consistency requirement of `CLAUDE.md` §13 and README §8.2 following entries 088 and 089. | Documentation Manager (AI-assisted) | Not Submitted |
| 091 | 2026-08-20 | CMP-CTRL-INDEX / STATUS / CHANGELOG | Control files | 3.8 | **`CC-029` opened and closed in the same entry**, recording an interpretation the Project Owner ruled on rather than leaving it in a commit message. `BE-173` asks for a policy change to be evidenced with the previous and new value; `BE-107` ‡ fixes the evidential record at six fields and `BE-201` ‡ forbids the free-form field a value would arrive in. The agent applied the narrow reading during `CMP-IMP-031` — the versioned record of `DB-152` holds the values, the evidential record of `DB-154` names the actor and points at the one version row — and **reported it rather than deciding it**. The Project Owner accepted that reading on 2026-08-20 and directed that **no seventh field be added and CMP-DOC-09 §9 not be modified** unless a future authoritative decision requires the values inside the evidential record. **`CC-028` remains Open by explicit instruction**: the `.docx` renderings of CMP-DOC-13 and CMP-DOC-19 are **not** to be generated or modified, nor is `CMP-Implementation-Tracker.docx`, until the Project Owner authorises that work, and `CC-028` is **not to be closed merely because the Markdown source is correct**. The Markdown documentation may continue to be updated through this process where required. **No formal document was created, modified or approved by this entry, and no work item status was changed.** | Project Owner instruction of 2026-08-20: *"Your narrow interpretation is accepted … Record the interpretation/evidence where appropriate in the implementation/change-control record so that the ambiguity is not silently lost"*, and *"Keep `CC-028` Open … Do not mark CC-028 closed merely because the source documents are correct."* | Documentation Manager (AI-assisted) | Not Submitted |
| 092 | 2026-08-20 | **CMP-DOC-10** | **API Specification** | **0.1 → 0.2** | **Three implementation readings ratified and recorded in §5, §6 and §8.** Building `FEAT-035` reached three places where this document requires something without stating how it is met, and an implementation had to choose in order to meet it at all. Each was reported rather than decided quietly, and the Project Owner ratified all three on 2026-08-20. **`API-217`** — the response envelope. `API-022` (the version that served the response), `API-023` (deprecation), `API-043` ‡ (the evaluation time) and `API-064` (a replay indication) each require a marking and none says where it goes; all four now sit in one `meta` object beside `data`, because the alternative was four separate inventions. `API-035` is unaffected — a resource still has exactly one representation, and the envelope describes the **response**. `API-029` already makes an added `meta` field non-breaking. The four error-branch shapes of `API-072` ‡ are unchanged and carry no `data`. **`API-218`** — `426 Upgrade Required` for the version-unsupported outcome. §5 describes the outcome without a status and §8.6 places it outside the four branches; `API-024` ‡’s actual constraint is that it must **not** be a not-found outcome, which is preserved and restated. **`API-219`** — the scope of `API-071` ‡. A request naming no operation (an unrouted path, or a method the platform does not route on a path it does) is not an operation failing, so the framework’s own not-found and method-not-allowed responses are not reshaped into the four branches. `API-094` ‡ is unaffected: it concerns a **resource** the caller may or may not be entitled to, which an unrouted path is not. A **FACT** paragraph records that this is a reading and not a licence — the Project Owner directed that **no fallback handler be introduced** to force unrouted requests into the four branches, and that the position be revisited through change control should an authoritative specification later establish that `API-071` ‡ reaches them. Statistics updated: §1.1 and §20.1 to 219 statements (`API-001` … `API-219`), sources named 219 of 219, §5 from 16 to 17, §6 from 22 to 23, §8 from 24 to 25, total 219; §19 self-check items 8 and 9 restated, item 9 recording that three sections now hold an identifier outside their original block; §20.2 restated as 100 of 219 (still 46%); Appendix A given three rows. **No existing statement was altered, no ‡ marking changed, no operation, resource, field or status was added, the document remains Draft, and the `.docx` rendering is NOT regenerated — `CC-028` remains Open by standing instruction.** | Project Owner instruction of 2026-08-20: *"I ratify the following implementation decisions as currently implemented"* — the `meta`/`data` response envelope with the four error branches preserved and additive `meta` fields non-breaking; `426 Upgrade Required` with the prohibition on representing an unsupported version as `404` preserved — and *"Keep the current interpretation … Do not introduce a fallback handler merely to force unrouted methods into the four API failure branches … Record the interpretation and the observed framework behaviour so that it is explicit rather than silent."* | Solution Architect / Backend Lead (AI-assisted) | Not Submitted |
| 093 | 2026-08-20 | CMP-CTRL-INDEX / STATUS / CHANGELOG / RTM | Control files | Index, Status, Changelog 3.9; RTM 3.3 | **Control files brought into agreement with CMP-DOC-10 v0.2** (entry 092). Index §2 records the control-file versions and §3 records CMP-DOC-10 at **0.2 Draft**; Index §5 namespace allocation now reads `API-001` … `API-219` (219 issued, v0.2), noting the three sections the new identifiers were added to. Status §4, §5 and §7.2 updated to match. **A stale figure was also corrected**: Status §3 “Traceable IDs issued” still carried the v0.1 counts for `SEC`, `OPS` and `API` and therefore a total of 3,813; entries 088, 089 and 092 have since issued seven further statements, so the breakdown now reads 219 `API`, 242 `SEC` and 210 `OPS` and the total 3,820. The omission at entries 090 and 091 is corrected here rather than left standing. `Master_Traceability_Matrix.md` §4 identifier-integrity row for CMP-DOC-10 updated to v0.2 with its new range and count, recording that §5, §6 and §8 are no longer contiguous blocks because identifiers are never renumbered (Index §6); the chain-total NOTE added at entry 090 is restated to 3,628 statements. **CMP-DOC-20 is still not revised**, and its own figures continue to read as at issue. **`CC-030` opened and closed in the same entry**, recording the observed framework behaviour behind `API-219`: the routing layer answers a method it does not route with `405` before any interface middleware runs, so an unrouted request never reaches the platform’s own error mapping. The Project Owner directed that this be recorded rather than engineered around. **`CC-028` remains Open**; no `.docx` was generated or modified. **No formal document was created or approved by this entry, and no work item status was changed.** | Consistency requirement of `CLAUDE.md` §13 and README §8.2 following entry 092. | Documentation Manager (AI-assisted) | Not Submitted |
| 094 | 2026-08-20 | **CMP-DOC-01** | **Business Analysis Document** | **0.1 → 0.2** | **Three account decisions taken by the Project Owner, and two decisions partially resolved.** **`BAD-RULE-006`** gains the verification standing vocabulary: **`UNVERIFIED` and `VERIFIED`**, where an account is `VERIFIED` once control of its registered phone number has been demonstrated and `UNVERIFIED` until then, following the flow of `FRD-FR-007`, `FRD-FR-008` and `FRD-FR-018`. The rule remains **Absolute — FACT**. **`BAD-RULE-010`** moves out of `[TBD – BAD-DEC-006]`: the permitted account states are **`ACTIVE`, `SUSPENDED` and `DEACTIVATED`**; only `ACTIVE` permits normal authenticated use; a `SUSPENDED` or `DEACTIVATED` account **shall not regain an active session** other than through a defined account-state transition. **Who may perform that transition, on what grounds and with what appeal is expressly still undecided** and is recorded in the rule text itself against `FRD-GAP-024`, `BAD-DEC-006` and `BAD-DEC-016`. **`BAD-RULE-043` is issued** — the first new business rule since v0.1 — fixing the mandatory identifying detail for registration at **the verified phone number and nothing else**, and stating that name, email address, date of birth, address and other profile attributes are **not collected at registration**. It answers the deferral in `FRD-FR-002` and records that a later decision may add an attribute but that none may be added on the ground that it may be useful. **`BAD-RULE-008`** is narrowed rather than closed: phone-number standing is now `BAD-RULE-006`’s, and what verification levels exist **beyond** it, what evidence each requires and what each permits stay `[TBD – BAD-DEC-005]`. **`BAD-RULE-011`** is annotated as subsumed in effect by `BAD-RULE-010` and retained, because it states the business intent at a level that survives a change to the state model; its class is unchanged. §27 decision register: **`BAD-DEC-005` and `BAD-DEC-006` are both marked PARTIALLY RESOLVED**, each naming precisely what it still does not answer, and both remain open at their original priority. **A pre-existing discrepancy in §14.9 is reported rather than corrected.** The rule set is now 43, but the four bucket counts do not sum to the register as issued at v0.1 — the class column carries six distinct forms and the summary has four buckets, and a mechanical count of rows whose class is a `[TBD]` marker gives **13, not the 11 the summary states**. Recomputing would require the author’s intent for the buckets, which is not recorded, so a dated NOTE states the position and the counts are left untouched. **No other rule was altered, no requirement was created, and the document remains Draft. The `.docx` rendering is NOT regenerated — `CC-028` remains Open by standing instruction.** | Project Owner instruction of 2026-08-20 recording three decisions for `FEAT-004`: the verification standing vocabulary (*"`UNVERIFIED` … `VERIFIED`"*), the account state model (*"`ACTIVE` … `SUSPENDED` … `DEACTIVATED`"* with *"a terminated/deactivated account must not regain an active session without the explicitly defined account-state transition process"*), and the mandatory identifying detail (*"Keep the initial account identity deliberately narrow … verified phone number … Do not add name, email address, date of birth, address, or other profile attributes"*). | Business Analyst (AI-assisted) | Not Submitted |
| 095 | 2026-08-20 | **CMP-DOC-04** | **Functional Requirements Document** | **0.1 → 0.2** | **Two functional gaps partially closed and one deferral answered, following entry 094.** `FRD-GAP-002` is closed for the **verification standing vocabulary** by `BAD-RULE-006` and remains open for **driver eligibility** on `BAD-DEC-005`. `FRD-GAP-003` is closed for the **account state model** by `BAD-RULE-010`; account state **change and appeal** was already registered separately at `FRD-GAP-024` and remains open there. Each row is struck through only for the half that is closed, with the remainder stated beside it. **No functional requirement text was altered.** `FRD-FR-002` continues to read *"the identifying details the business defines as mandatory"* and deliberately so: it defers to the business, the business has now answered at `BAD-RULE-043`, and the requirement will still be correct if a later decision adds an attribute. `CLAUDE.md` §3 puts a business rule in CMP-DOC-01, so the answers are held there and cited here rather than copied. A dated **FACT** paragraph beneath §9.3 records the three decisions, where they are held, and the convention that a struck-through gap is closed while the text beside it is what still is not. **The gap count is unchanged at 29 and the Critical count at eleven**, because a partially closed gap is not a removed one. **No requirement was created, altered or withdrawn; no ‡ marking changed; the document remains Draft; the `.docx` is NOT regenerated — `CC-028` remains Open.** | Consequence of entry 094: CMP-DOC-04 §9.3 is the gap register, and two of its rows became partly answerable. | Product Analyst (AI-assisted) | Not Submitted |
| 096 | 2026-08-20 | CMP-CTRL-INDEX / STATUS / CHANGELOG / RTM | Control files | Index, Status, Changelog 4.0; RTM 3.4 | **Control files brought into agreement with CMP-DOC-01 v0.2 and CMP-DOC-04 v0.2** (entries 094 and 095). Index §2 records the control-file versions and §3 records both documents at **0.2 Draft**; Index §5.1 records `BAD-RULE-` at **43 issued** (42 at v0.1; `BAD-RULE-043` at v0.2). Status §4 and §5 updated to match. `Master_Traceability_Matrix.md` §4 identifier-integrity rows for CMP-DOC-01 and CMP-DOC-04 updated to v0.2. **Neither document’s `BAD-BR-` or `FRD-FR-` range changed** — entry 094 issued a `BAD-RULE-`, which §5.1 of the Index records as an **auxiliary prefix outside the forward traceability chain**, and entry 095 issued nothing. The chain-total NOTE is therefore unchanged at 3,628 statements. **`CC-028` remains Open**; no `.docx` was generated or modified. **No formal document was approved, and no work item status was changed — the tracker’s Notes column still carries no blocker flag for `CMP-IMP-051`–`059`, which is reported to the Project Owner rather than edited.** | Consistency requirement of `CLAUDE.md` §13 following entries 094 and 095. | Documentation Manager (AI-assisted) | Not Submitted |
| 097 | 2026-08-20 | **CMP-DOC-13** | **Security Design** | **0.2 → 0.3** | **`SEC-025` decided by the Project Owner: five attempts per phone number per hour, and no account lockout.** `SEC-024` holds attempt limits, windows **and lockout behaviour** as policy configuration; all three now have a value and the third is **none**. A dated **FACT** in §5.2 records two readings, stated rather than assumed because `SEC-022` ‡ and `SEC-023` ‡ are both integrity-critical. **First**, the single bound serves both: `SEC-023` ‡ bounds verification attempts against a number and `SEC-022` ‡ bounds authentication attempts against an account, but `SEC-015` makes authentication **the** demonstration of possession of a verified number — the attempt is the same attempt and the number is the account’s — and the decision’s *"no account lockout"* settles the account dimension in terms. No separate account-level bound is introduced and none is invented. **Second**, no lockout means **no locked-account state and therefore no column holding one**: a rate bound is a question asked of the attempts `DB-043` retains *"so that an attempt limit is enforceable against state rather than against memory"*, which is what that statement already required. §23.1 `[TBD – Business Decision Required]` markers reduced from 21 to 20. **The four remaining authentication values are named as still undecided** — `SEC-017` demonstration lifetime, `SEC-031` hash cost parameters, `SEC-039` session lifetime, `SEC-049` concurrent session limit — and no behaviour depending on any of them is built. **No statement other than `SEC-025` was altered, no ‡ marking changed, the document remains Draft, and the `.docx` is NOT regenerated — `CC-028` remains Open.** | Project Owner instruction of 2026-08-20: *"SEC-025: 5 attempts per number per hour, no account lockout"*. | Security Analyst (AI-assisted) | Not Submitted |
| 098 | 2026-08-20 | CMP-CTRL-INDEX / STATUS / CHANGELOG / RTM | Control files | Index, Status, Changelog 4.1; RTM 3.5 | **Control files brought into agreement with CMP-DOC-13 v0.3** (entry 097). Index §2 and §3 and Status §4 and §5 record the new versions. **No identifier was issued** — entry 097 decided the value of an existing statement — so Index §5’s `SEC-` allocation is unchanged at `SEC-001` … `SEC-242`, the RTM row moves to v0.3 with the same range and count, and the chain-total NOTE is unchanged at 3,628 statements. **`CC-028` remains Open**; no `.docx` was generated or modified. **No formal document was approved and no work item status was changed.** | Consistency requirement of `CLAUDE.md` §13 following entry 097. | Documentation Manager (AI-assisted) | Not Submitted |
| 099 | 2026-08-20 | **CMP-DOC-04** | **Functional Requirements Document** | **0.2 → 0.3** | **`FRD-FR-013`’s restart interpretation ratified by the Project Owner.** A registration restarted with a number already held by an **unverified** account **reuses that account** rather than creating a second `op_users` row, and no duplicate account is created for one phone number. A dated **FACT** in §4.1 records the reconciliation rather than asserting the reading: `UC-001` A2 says the abandoned account *"remains unverified and unusable"* and that the person *"may restart registration with the same number"* — it never says a second account is created, and step 5’s *"creates the account"* belongs to the main path. `FRD-FR-004`’s *"already registered to an **active** account"* is read as **an account that can be logged into**, because A1 offers the login path and `FRD-FR-018` routes an unverified user to verification rather than into the application; offering login to an unverified account would contradict it. **The schema already enforces the ratified position** — `op_users_phone_number_unique`, integrity constraint 22, was added at `CMP-IMP-051` and makes a duplicate account impossible — **so no schema change follows from this entry**. **A gap the reading exposes is reported and left open.** Neither `UC-001` nor any requirement says what happens when registration is attempted with a number held by a `SUSPENDED` or `DEACTIVATED` account: reuse would let a suspended person regain access, which `BAD-RULE-010` forbids, and a second account is forbidden by this ratification and prevented by the constraint. The case **did not exist before `BAD-RULE-010` was decided** earlier the same day, which is why no flow covers it. Recorded at **`CC-031`, Open**. **No requirement was created, altered or withdrawn, no ‡ marking changed, the gap register’s count is unchanged at 29, the document remains Draft, and the `.docx` is NOT regenerated — `CC-028` remains Open.** | Project Owner instruction of 2026-08-20: *"For `FRD-FR-013`, ratify the following interpretation: A restarted registration using an existing phone number reuses the existing unverified account rather than creating a second `op_users` row. Do not create duplicate user accounts for the same phone number. If the registration specification requires a different lifecycle or transition, stop and report the conflict before modifying the schema."* | Product Analyst (AI-assisted) | Not Submitted |
| 100 | 2026-08-20 | CMP-CTRL-INDEX / STATUS / CHANGELOG / RTM | Control files | Index, Status, Changelog 4.2; RTM 3.6 | **Control files brought into agreement with CMP-DOC-04 v0.3** (entry 099), and **`CC-031` opened**. Index §2 and §3 and Status §4 and §5 record the new versions; the RTM row moves to v0.3 with the same range and count. **No identifier was issued** — entry 099 recorded an interpretation of an existing requirement — so Index §5’s `FRD-FR-` allocation is unchanged at 260 and the chain total stays 3,628. **`CC-031` is Open, not closed.** It records the one registration case with no specified behaviour, which `BAD-RULE-010` created on the day it was decided. **`CC-028` also remains Open**; no `.docx` was generated or modified, and no work item status was changed. | Consistency requirement of `CLAUDE.md` §13 following entry 099. | Documentation Manager (AI-assisted) | Not Submitted |

## 4. Approval Register

| Document ID | Document | Version | Approved By | Approval Date | Notes |
|---|---|---|---|---|---|
| — | — | — | — | — | No document has been approved. |

> **FACT.** As of 2026-08-16 no document in this repository has been approved by the
> Project Owner. All artifacts are `Draft`. CMP-DOC-01 (BAD), CMP-DOC-02 (BRD) and
> CMP-DOC-03 (USECASE), CMP-DOC-04 (FRD), CMP-DOC-05 (NFR), CMP-DOC-06 (SRS), CMP-DOC-07 (SAD) and CMP-DOC-08 (MOBILE), all at v0.1, have been **issued for review**; none is
> `Under Review` in the lifecycle sense until the Project Owner confirms review has
> commenced.
>
> **Chain depth warning.** **Eight** documents now depend on an unapproved baseline.
> CMP-DOC-04 rests on CMP-DOC-03, which rests on CMP-DOC-02, which rests on CMP-DOC-01.
> A change to CMP-DOC-01 in review propagates through three successors and into build
> instructions and now into the allocation from which architecture derives. See conflict
> entries **CC-002** … **CC-008**.

## 5. Conflict & Change Control Register

Conflicts between a new requirement and an already-approved document are recorded here
before any affected document is modified.

| # | Date | Conflict | Documents Affected | Impact | Decision Required From | Decision | Status |
|---|---|---|---|---|---|---|---|
| CC-001 | 2026-08-16 | **Identifier ambiguity.** `README.md` §9.3 (from Master Documentation Control Prompt §20) allocates the single prefix `BAD-BR-` to the Business Analysis Document without distinguishing business *requirements* from business *rules*, personas, risks, KPIs and other element types. CMP-DOC-01 §0.10 resolves this by reserving `BAD-BR-` for traceable business requirements and introducing 15 auxiliary prefixes. | `README.md`, `Documentation_Index.md`, `CMP-DOC-01` | Low if confirmed. If the Project Owner rejects the convention, CMP-DOC-01's auxiliary identifiers require renumbering under change control. Traceable `BAD-BR-` IDs are unaffected either way. | Project Owner | **Pending** — tracked as `BAD-DEC-024` | Open — deviation recorded, **no approved document has been silently modified** |
| CC-002 | 2026-08-16 | **Workflow deviation — BRD produced from an unapproved predecessor.** `README.md` §8 and Master Documentation Control Prompt §27 Step 5 expect a document to be produced from *approved* prerequisites. CMP-DOC-02 was authored while CMP-DOC-01 remained at status `Draft` v0.1, on the Project Owner's explicit instruction. | `CMP-DOC-01`, `CMP-DOC-02`, `Documentation_Status.md` | **Medium.** Every requirement in CMP-DOC-02 inherits the confidence level of its source statement in CMP-DOC-01. If CMP-DOC-01 changes during review, CMP-DOC-02 must be revised and affected requirements re-examined. CMP-DOC-02 must not be baselined before CMP-DOC-01 is approved. | Project Owner | **Taken** — Project Owner directed that the BRD be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-02 §0.8.2 and `BRD-RISK-001`; **no approved document has been modified** |

| CC-003 | 2026-08-16 | **Workflow deviation — Use Case Specification produced from two unapproved predecessors.** CMP-DOC-03 was authored while both CMP-DOC-01 and CMP-DOC-02 remained at status `Draft` v0.1, and while 79 of 188 business requirements (42%) remained blocked by 17 unresolved business decisions. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`, `CMP-DOC-02`, `CMP-DOC-03`, `Documentation_Status.md` | **Medium–High.** The dependency chain is now three documents deep on an unapproved baseline. 33 of 83 use cases could not be specified and are recorded as Outlined with the blocking decision named; **no flow was invented to fill a gap**. If either predecessor changes in review, CMP-DOC-03 must be revised. It must not be baselined until both predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the use cases be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-03 §0.8.2 and `UC-RISK-001`; **no approved document has been modified** |

| CC-004 | 2026-08-16 | **Workflow deviation — Functional Requirements produced from three unapproved predecessors.** CMP-DOC-04 was authored while CMP-DOC-01, CMP-DOC-02 and CMP-DOC-03 all remained at status `Draft` v0.1, and while 33 of 83 use cases remained Outlined. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-04`, `Documentation_Status.md` | **High.** The dependency chain is now four documents deep and has reached build instructions. 33 use cases could not be decomposed; 29 functional gaps are recorded in place of requirements and **no behaviour was invented to close one**. Two gaps (`GAP-008`, `GAP-009`) are live product defects reachable through specified behaviour. CMP-DOC-04 must not be baselined for construction until all three predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the FRD be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-04 §0.8.2 and `FRD-RISK-001`; **no approved document has been modified** |

| CC-005 | 2026-08-16 | **Workflow deviation — Quality Attributes produced from four unapproved predecessors.** CMP-DOC-05 was authored while CMP-DOC-01 … CMP-DOC-04 all remained at status `Draft` v0.1, and while `BAD-DEC-018` (KPI and quality targets) remained unresolved. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-05` | **Medium.** Lower than `CC-004`: the quality attribute model depends on the *shape* of the system rather than on its undecided commercial policy, and 93 of 162 requirements are enforceable without any further decision. **No target value was invented** — 47 numeric targets are explicitly deferred to `BAD-DEC-018` with a route to setting each. CMP-DOC-05 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the NFR be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-05 §0.8.2 and `NFR-RISK-001`; **no approved document has been modified** |

| CC-006 | 2026-08-16 | **Workflow deviation — Software Requirements Specification produced from five unapproved predecessors.** CMP-DOC-06 was authored while CMP-DOC-01 … CMP-DOC-05 all remained at status `Draft` v0.1. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-06` | **High.** This is the document from which architecture derives, and the chain beneath it is six documents deep and unapproved. Allocation is nonetheless complete and verifiable (422 of 422), and 6 requirements with no upstream counterpart are disclosed in §11.5 rather than introduced silently. CMP-DOC-06 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the SRS be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-06 §0.8.2 and `SRS-RISK-001`; **no approved document has been modified** |

| CC-007 | 2026-08-16 | **Workflow deviation — System Architecture produced from six unapproved predecessors.** CMP-DOC-07 was authored while CMP-DOC-01 … CMP-DOC-06 all remained at status `Draft` v0.1, and while `BAD-DEC-018` left 69 quality targets unset. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-07` | **High.** Architecture derived from an unapproved requirement baseline inherits its instability, and construction derives from architecture. Mitigated by explicit statement of the 12 drivers, so that a change to any predecessor has a traceable structural impact. **Structure is decided; sizing is not** — 11 sizing decisions are deferred with named blockers rather than guessed, and no capacity, latency, availability or cost figure appears in the document. CMP-DOC-07 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the SAD be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-07 §0.8.2 and `ARCH-RISK-001`; **no approved document has been modified** |

| CC-008 | 2026-08-16 | **Workflow deviation — Mobile Architecture produced from seven unapproved predecessors.** CMP-DOC-08 was authored while CMP-DOC-01 … CMP-DOC-07 all remained at status `Draft` v0.1, and while every mobile resource budget in `NFR-151`–`158` remained unset. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-08` | **Medium.** Lower than `CC-007`: the client architecture derives chiefly from the 14 client obligations in CMP-DOC-07 §6.1, which are stable structural statements, and all 14 are realised. **No resource budget was invented** — 9 measurement points are named instead. Two decisions held elsewhere remain outstanding and are recorded as `MOB-OQ-001` (device range) and `MOB-OQ-002` (accessibility standard). CMP-DOC-08 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Mobile Architecture be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-08 §0.8.2 and `MOB-RISK-001`; **no approved document has been modified** |

| CC-009 | 2026-08-17 | **Workflow deviation — Backend Architecture produced from eight unapproved predecessors.** CMP-DOC-09 was authored while CMP-DOC-01 … CMP-DOC-08 all remained at status `Draft` v0.1, and while launch scale remained unstated. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-09` | **High.** The backend architecture is what CMP-DOC-10, CMP-DOC-11 and CMP-DOC-17 derive from, and it rests on an unapproved eight-document baseline. A distinct and separable exposure is recorded: **three architectural assumptions (`BE-ASM-01`, `BE-ASM-02`, `BE-ASM-03`) rest on a launch-scale figure nobody has supplied**, and CMP-DOC-11's index and partitioning strategy will need the same figure. **No capacity, latency, pricing or retention value was invented** — 11 tuning values are held as configuration with their decisions named. CMP-DOC-09 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Backend Architecture be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-09 §0.8.2 and `BE-RISK-01`…`08`; **no approved document has been modified** |

| CC-010 | 2026-08-17 | **Workflow deviation — API Specification produced from nine unapproved predecessors.** CMP-DOC-10 was authored while CMP-DOC-01 … CMP-DOC-09 all remained at status `Draft`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-10` | **High, and of a different kind to `CC-002`…`CC-009`.** Those concerned analysis and architecture documents, which are read and interpreted. **This is the first document that will be implemented literally**: a developer builds the endpoint the specification names, and a client calls it. An error here becomes running code rather than a misunderstanding. Mitigated by discharging all four inherited obligations explicitly (§17.2, §17.3), by resolving every citation against source text before issue, and by naming 11 withheld resources rather than inventing operations for undecided behaviour. **One invented resource was caught and removed before issue**: a recurring-commute resource had been specified for an area CMP-DOC-04 records as carrying zero functional requirements. CMP-DOC-10 must not be baselined for construction until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the API Specification be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-10 §0.8.2 and `API-RISK-01`; **no approved document has been modified** |

| CC-011 | 2026-08-17 | **Workflow deviation — Database Design produced from ten unapproved predecessors.** CMP-DOC-11 was authored while CMP-DOC-01 … CMP-DOC-10 all remained at status `Draft`, and while launch scale remained unstated. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-11` | **High, and the most expensive of the eleven to be wrong.** A schema is the hardest artefact in this chain to change once it holds data: an interface can be versioned and an application can be redeployed, but a populated table must be migrated. A separable exposure is recorded: **7 sizing decisions in §14.5 cannot be taken without launch scale (`GAP-016`), and 4 of them govern the three tables that grow without bound** — deferring them past launch converts each from a choice into a migration. **No retention period, recovery objective, volume or index-beyond-a-stated-access-path was invented**, and 7 tables were withheld rather than speculated. CMP-DOC-11 must not be baselined for construction until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Database Design be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-11 §0.8.2 and `DB-RISK-01`; **no approved document has been modified** |

| CC-012 | 2026-08-17 | **Workflow deviation — Security Design produced from eleven unapproved predecessors, and out of sequence.** CMP-DOC-13 was authored while CMP-DOC-01 … CMP-DOC-11 all remained at status `Draft`, and while **CMP-DOC-12 does not exist**. Produced on the Project Owner's explicit instruction, following the recommendation at CMP-DOC-07 `AR-09`. | `CMP-DOC-01`…`CMP-DOC-11`, `CMP-DOC-13` | **High, with one mitigating feature and one aggravating one.** Mitigating: producing it early was the *recommended* course — `AR-09` identified four unmechanised security properties and two were prerequisites for the evidential log, so deferring this document to its sequence position would have meant building the evidential log without its mechanism. Aggravating: **this document supplies mechanisms that five predecessors deferred to it**, so an error here propagates backwards into documents already written rather than only forwards. A distinct exposure is recorded: **the regulatory position is unknown (`SEC-OQ-01`) and no default is assumed**, so nothing in this design contradicts a regime and nothing is validated against one either. The out-of-sequence production means CMP-DOC-12 must take §7.3 and `SEC-160` as inputs when it is written. CMP-DOC-13 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Security Design be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-13 §0.8.2 and `SEC-RISK-01`; **no approved document has been modified** |

| CC-013 | 2026-08-17 | **Workflow deviation — UI/UX Specification produced from twelve unapproved predecessors.** CMP-DOC-12 was authored while CMP-DOC-01 … CMP-DOC-11 and CMP-DOC-13 all remained at status `Draft`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-13` | **High, and of a different character again.** This is the document users actually experience: an error here is not a misunderstanding or a defect but a wrong thing shown to a person at the moment they are deciding whether to get into a stranger's car. Two design decisions carry the most weight and both are deliberate costs: `UXDR-06` blocks booking on a poor connection rather than permit commitment on cached verification standing, and `UXDR-13` ships no emergency control rather than promise a response nobody has specified. **A material finding emerged during authoring**: resolving the screen inventory against CMP-DOC-03's actual use case tiers showed that **every user-facing route to raising a safety matter is Outlined** — SOS (`BAD-DEC-011`), non-emergency reporting (`BAD-DEC-016`) and trip sharing (`BAD-DEC-022`) — so the platform is ready to receive a safety signal the interface has no specified way to send. CMP-DOC-12 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the UI/UX Specification be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-12 §0.8.2 and `UX-RISK-01`; **no approved document has been modified** |

| CC-014 | 2026-08-17 | **Identifier omission.** Master Documentation Control Prompt §20 lists a traceable identifier prefix for every formal document **except CMP-DOC-12**. The prefixes named are `BAD-BR`, `BRD-REQ`, `UC`, `FRD-FR`, `NFR`, `SRS-REQ`, `ARCH`, `MOB`, `BE`, `API`, `DB`, `SEC`, `PAY`, `GPS`, `NOTIF`, `ADM` and `TC` — seventeen prefixes for eighteen documents requiring one. CMP-DOC-12 §0.10 resolves this by adopting `UX-`, chosen to be distinct from `UC-`. | `README.md`, `Documentation_Index.md`, `CMP-DOC-12` | **Low if confirmed.** If the Project Owner prefers a different prefix, 224 identifiers require renumbering under change control before any downstream document cites them. **No downstream document cites `UX-` yet**, so the cost of changing it is at its minimum now and rises with each subsequent document. Follows the precedent of `CC-001`. | Project Owner | **Pending** — confirmation of the `UX-` prefix sought | Open — deviation recorded, **no approved document has been modified** |

| CC-015 | 2026-08-17 | **Workflow deviation — Payment & UPI Specification produced from thirteen unapproved predecessors, and with the payment provider unchosen.** CMP-DOC-14 was authored while CMP-DOC-01 … CMP-DOC-13 all remained at status `Draft`, and while `BE-161` records three of five providers as `[TBD – Business Decision Required]`, the payment provider among them. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-14` | **High, and the highest-consequence of the fifteen.** This is the money path. Mitigating: the receipt-and-verification half is the most completely specified behaviour in the chain — 24 functional requirements realised and three zero-tolerance quality requirements achieved by construction — and it was specifiable because `BAD-RULE-032` and `BAD-RULE-033` are absolute and decided. Aggravating, and separably recorded: **six money decisions are unresolved**, so the platform can take money from day one and has **no specified way to pay anyone** (`PAY-RISK-06`, severity 20); and **`PAY-175` — a verified payment against a seat that has become unavailable — is reachable today through fully specified behaviour with no defined resolution** (`PAY-RISK-05`, severity 20). A third exposure: **`SADR-10` depends entirely on a provider property that cannot be confirmed until a provider is chosen**, which §14.3 converts from an integration discovery into a selection gate. **No provider, scheme rule, fee, rate, settlement schedule or refund policy was invented.** CMP-DOC-14 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Payment & UPI Specification be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-14 §0.8.2 and `PAY-RISK-01`; **no approved document has been modified** |

| CC-016 | 2026-08-17 | **Workflow deviation — GPS / Live Trip Specification produced from fourteen unapproved predecessors, and with the trip state model undecided.** CMP-DOC-15 was authored while CMP-DOC-01 … CMP-DOC-14 all remained at status `Draft`, and while `UC-041` (*progress trip state*) remains **Outlined** under `BAD-DEC-015`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-15` | **Medium-High.** Lower than `CC-015`: tracking carries no money and no absolute rule depends on a position — `GADR-08` makes tracking an enhancement rather than a precondition, so a defect degrades information and never validity. Two separable exposures are recorded. First, **the trip state model does not exist**: `GADR-14` specifies the invariants that hold under any model and names no state, which means a developer cannot build the trip state display from this document alone (`GPS-156`). Second, and newly found: **pickup and drop sequencing on a multi-passenger trip is undecided and was not previously recorded anywhere in the chain** — registered as `GAP-017`. `FRD-FR-159` requires the driver to see the estimate to their next pickup or drop, and nothing states in what order those points are visited. **No cadence, staleness, accuracy, battery, data or cost figure was invented; 11 values are named unset.** CMP-DOC-15 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the GPS / Live Trip Specification be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-15 §0.8.2 and `GPS-RISK-01`; **no approved document has been modified** |

| CC-017 | 2026-08-17 | **Workflow deviation — Communication & Notification Specification produced from fifteen unapproved predecessors, and with the inter-user privacy boundary undecided.** CMP-DOC-16 was authored while CMP-DOC-01 … CMP-DOC-15 all remained at status `Draft`, and while `BAD-DEC-022` (*privacy boundaries between users*) remains unresolved, leaving `UC-045` **Partial**. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-16` | **Medium-High.** The notification half is complete and its governing rule is a prohibition — `NOTIF-133`, no business value — which is structurally safe. The messaging half is **mechanism without a boundary**: conversation formation, recording, delivery, offline behaviour and retrieval are fully specified, and **what one participant sees of another is not specified anywhere**, so a developer cannot build the conversation header (`NOTIF-170`). A separable exposure is recorded and is new: **`NOTIF-176`** — with one delivery channel, `NFR-076`'s guarantee that a safety notification is not suppressed by preference says nothing about it reaching a device that is switched off, and no requirement asks for a second channel. **No disclosure rule, retention period, retry figure, second channel, reminder, digest, read receipt or moderation capability was invented.** CMP-DOC-16 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Communication & Notification Specification be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-16 §0.8.2 and `NOTIF-RISK-01`; **no approved document has been modified** |

| CC-018 | 2026-08-17 | **Workflow deviation — Admin / Filament Specification produced from sixteen unapproved predecessors, and with the administrative role set undecided.** CMP-DOC-17 was authored while CMP-DOC-01 … CMP-DOC-16 all remained at status `Draft`, and while `SEC-063` records the administrative role definitions as `[TBD – Business Decision Required]`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-17` | **High, and it carries the chain's highest single risk.** This document exists because CMP-DOC-06 rated administrative rule duplication `SRS-RISK-003` at severity 9 — the highest integrity risk in the requirements chain — and CMP-DOC-09 responded with `BE-074`–`BE-082` and the words *without exception*. `ADM-RISK-03` restates it at the point it would occur and rates it **25**. The response is structural: `ADM-002` forbids the Eloquent binding and obligations 1 and 2 are non-suppressible in every environment. **The cost is recorded rather than hidden**: `ADMR-01` states that Filament's productivity advantage is substantially forfeited. Three separable exposures: **the role set does not exist and blocks deployment** (`ADM-168`), not merely a feature; **five of twelve operator capabilities are withheld**, two of them core (`ADM-195` verification adjudication, `ADM-196` account state change); and the surface depends on application service operations CMP-DOC-09 does not yet enumerate (`ADMR-14`, §17.7). **No role, override, measure target or withheld capability was invented.** CMP-DOC-17 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Admin / Filament Specification be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-17 §0.8.2 and `ADM-RISK-01`; **no approved document has been modified** |

| CC-019 | 2026-08-17 | **Workflow deviation — Testing & QA Documentation produced from seventeen unapproved predecessors.** CMP-DOC-18 was authored while CMP-DOC-01 … CMP-DOC-17 all remained at status `Draft`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-18` | **Medium, and of an unusual kind.** A test suite written against an unapproved specification verifies conformance to a document that may change — which is still worth doing and is not the same as verifying the product is right (`TC-RISK-01`). The document's value is largely independent of approval status: consolidating 99 obligations from nine lists, identifying the 25 non-suppressible, and reconciling the 13 structural rules are useful whether or not the sources are baselined. **A material finding emerged from the consolidation itself**: `TC-014` records that **99 obligations do not cover 1,164 integrity-critical statements**, which no predecessor could have found because no predecessor could see the union. `TC-017` creates the mapping work and `TC-020` forbids a coverage figure until it is done. Two further exposures: **five verification categories cannot be pass-or-fail** because every performance, capacity, availability and resource target in the chain is unset (`GAP-012`); and **everything withheld across four documents has no test**, so `TC-181` records that a fully passing suite leaves the largest known risks untouched. **No coverage target, threshold, tool or test for undecided behaviour was invented.** CMP-DOC-18 must not be baselined until its predecessors are approved. | Project Owner | **Taken** — Project Owner directed that the Testing & QA Documentation be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-18 §0.8.2 and `TC-RISK-01`; **no approved document has been modified** |

| CC-020 | 2026-08-17 | **Workflow deviation — DevOps / Deployment Documentation produced from eighteen unapproved predecessors.** CMP-DOC-19 was authored while CMP-DOC-01 … CMP-DOC-18 all remained at status `Draft`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-19` | **Medium, and compounded by a second condition.** The usual exposure applies — obligations may change under the specification (`OPS-RISK-01`). The unusual one is that **this document could not have been completed even from an approved chain**: `OPS-014` records that deployment is the first place where structure and quantity stop being separable, and **21 sizing decisions have no figure**. Eleven trace to `BAD-DEC-018`, five to `GAP-016`, one to `BAD-DEP-009`, and three arise here. **Two procedures the chain owes — restore-versus-retention (`DB-230`, `SEC-224`) and incident response (`SEC-216`, `SEC-177`) — are specified structurally and cannot be executed**, one blocked on eight unset retention periods and one on having no owner after six documents (`SEC-OQ-07`). `OPS-195` states the consequence plainly: a deployment built from this document alone would be unsized, unalerted, and unable to complete either procedure. **No provider, product, capacity figure, threshold or provisional default was invented.** CMP-DOC-19 must not be baselined until its predecessors are approved, and must not be provisioned from until the register is resolved. | Project Owner | **Taken** — Project Owner directed that the DevOps / Deployment Documentation be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-19 §0.8.2 and `OPS-RISK-01`; **no approved document has been modified** |
| CC-021 | 2026-08-17 | **No identifier prefix allocated for CMP-DOC-19.** §20 of the Master Documentation Control Prompt lists prefixes through `TC-` (CMP-DOC-18) and allocates none for CMP-DOC-19 or CMP-DOC-20. | `CMP-DOC-19`, Documentation_Index §5 | **Low.** Resolved on the precedent set by `CC-014`, where CMP-DOC-12 adopted `UX-`: the chain's convention is one prefix per document for traceable statements. `OPS-` was adopted. **`DEP-` was considered and rejected**, because `BAD-DEP-nnn` already denotes a dependency in CMP-DOC-01 and a word-boundary match on `DEP-` would capture it, breaking every automated cross-reference check. `OPS-` collides with nothing in the repository. | Documentation Manager | **Taken** — `OPS-` adopted and registered in the namespace allocation table. | Closed — recorded in CMP-DOC-19 §0.10 and Documentation_Index §5.19 |

| CC-022 | 2026-08-17 | **Workflow deviation — Requirements Traceability & Release Documentation produced from nineteen unapproved predecessors.** CMP-DOC-20 was authored while CMP-DOC-01 … CMP-DOC-19 all remained at status `Draft`. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-20` | **Medium, and self-limiting.** A traceability report over an unapproved chain reports the state of drafts, and `TRDR-14` requires regeneration whenever any document changes, so the exposure closes itself as the chain baselines. **The measurement was worth taking now regardless of approval status**, because it produced a finding no predecessor could have produced: `TR-034` records that **208 of 955 upstream requirements carry no downstream citation, 37 of them integrity-critical**, and `TR-053` explains why no per-document check could ever have detected it — a document can verify its own citations and cannot verify that it was cited. §16.7 places an obligation on CMP-DOC-02, 04, 05 and 06 **not to baseline until the 37 are reviewed**, so the finding is bound into the approval path rather than left as an observation. **No traceability link was created, no coverage figure reported, no release date or readiness percentage stated, and no business decision taken.** | Project Owner | **Taken** — Project Owner directed that the Traceability & Release Documentation be produced now. Deviation accepted and recorded. | Open — recorded in CMP-DOC-20 §0.8.2 and `TR-RISK-01`; **no approved document has been modified** |
| CC-023 | 2026-08-17 | **No identifier prefix allocated for CMP-DOC-20.** §20 of the Master Documentation Control Prompt lists prefixes through `TC-` (CMP-DOC-18) and allocates none for CMP-DOC-19 or CMP-DOC-20. | `CMP-DOC-20`, Documentation_Index §5 | **Low.** Resolved on the precedent of `CC-014` (`UX-`) and `CC-021` (`OPS-`): the chain's convention is one prefix per document for traceable statements. `TR-` was adopted and collides with nothing in the repository. | Documentation Manager | **Taken** — `TR-` adopted and registered in the namespace allocation table. | Closed — recorded in CMP-DOC-20 §0.10 and Documentation_Index §5.20 |
| CC-024 | 2026-08-19 | **Workflow deviation — implementation planning artefacts produced from twenty unapproved predecessors.** `CMP-PLAN-IMPL-TRACKER` and `CMP-PLAN-IMPL-ANALYSIS` decompose CMP-DOC-01 … CMP-DOC-20 into implementation work while all twenty remain at status `Draft` and none is approved. CMP-DOC-04 `FRD-RISK-001` and `FRD-ASM-002` both record that a changed source statement invalidates everything derived from it. Produced on the Project Owner's explicit instruction. | `CMP-DOC-01`…`CMP-DOC-20`, `CMP-PLAN-IMPL-TRACKER`, `CMP-PLAN-IMPL-ANALYSIS` | **High.** Any change to an approved-in-review source statement propagates into the work register. The register is a planning artefact and is expected to be reissued after baselining; **it must not be treated as a construction baseline until the chain is approved**. | Project Owner | Instruction given; artefacts produced. **No approved document was modified.** | Recorded |
| CC-025 | 2026-08-19 | **A ratings interface and table are specified for a functional area that carries zero requirements.** CMP-DOC-10 §11.9 lists `POST /trips/{id}/ratings` — *rate a counterparty* — citing `FRD-FR-172`, which is in fact a **messaging** requirement (*record every message against the conversation for its ride*). CMP-DOC-11 §6.7 and §6.10 list `op_trip_ratings` among the tables realising the `Trip` aggregate. **CMP-DOC-04 §1.2 and §9.2 record Ratings & Reviews as carrying zero functional requirements**, withheld pending `BAD-DEC-012`, and CMP-DOC-17 §15 withholds reputation records from the support surface. **A developer working from CMP-DOC-10 would build a withheld feature.** Discovered during implementation decomposition; not previously recorded. | `CMP-DOC-04`, `CMP-DOC-10`, `CMP-DOC-11`, `CMP-DOC-17` | **Medium.** No requirement exists to implement against, so anything built would encode a guess at `BAD-DEC-012`. Also carried as `DOC-CONFLICT-01` on the tracker `09_Decisions` sheet and as `RISK-018` on `11_Risks`. | Documentation Manager, then Project Owner | **Open.** This log records the conflict and does not resolve it; no document has been modified. Reconcile under change control before construction of the trip feature begins. | Open |
| CC-026 | 2026-08-19 | **The implementation register names a database account the database design does not define.** `CMP-IMP-047` in `CMP-Implementation-Tracker.xlsx` describes the three accounts as *"Application account; evidential-write account; read account unable to alter evidence"*. CMP-DOC-11 `DADR-09` §3.9 defines them as the **application**, **migration** and **read** accounts, and `DB-215` states that a migration runs under the migration account, which is the only account holding `DDL`. **No evidential-write account exists in the design** — `DB-118` ‡ gives the *application* account `SELECT` and `INSERT` on `ev_`, and no account is issued a separate evidential write privilege. A developer following the register alone would have created a fourth credential with write access to the evidential domain, which `DB-122` ‡ forbids in every environment. | `CMP-PLAN-IMPL-TRACKER` `CMP-IMP-047`; CMP-DOC-11 `DADR-09`, `DB-118` ‡, `DB-119` ‡, `DB-215` | The tracker is a planning artefact and creates no requirement (Index §3A), so the design governs and no specification statement is in doubt. The risk was that the register's wording is what a developer reads first. | Project Owner | **Resolved — Project Owner instruction of 2026-08-19: correct the tracker wording to DOC-11's defined account model (application, migration, read), and change no status column.** The subtask text of `CMP-IMP-047` was corrected accordingly. The implementation had already followed `DADR-09`. | Closed |
| CC-027 | 2026-08-19 | **A table name is written in the singular in two places where `DB-015` requires the plural.** CMP-DOC-11 `DB-015` states that table names *"shall be lower snake case, prefixed by domain, and plural"*. The §4 storage-domain diagram labels the table `idempotency_registry`, and `CMP-IMP-025` in the implementation register describes it as the *"mch_idempotency_registry table"* — both singular. | CMP-DOC-11 §4 diagram, `DB-015`; `CMP-PLAN-IMPL-TRACKER` `CMP-IMP-025` | Low, and **already resolved by the authoritative documentation**: `DB-015` is a numbered normative statement, while a diagram label and a planning-artefact description are neither. The table was created as **`mch_idempotency_registries`**. Recorded so that the two descriptive texts can be corrected, and so that the name is not later "fixed" to match them — a rename would be a destructive migration requiring explicit recorded approval under `DB-218` ‡. | Documentation Manager — a correction, not a decision | Follow `DB-015`. No decision is required of the Project Owner. | **Open — correction outstanding in CMP-DOC-11 §4 diagram and in the `CMP-IMP-025` register text.** |
| CC-028 | 2026-08-20 | **Dual-format requirement not satisfied at v0.2.** README §8.2 requires every formal document in both `.md` and `.docx`, and that *"Both must carry equivalent information"*. CMP-DOC-13 and CMP-DOC-19 were revised to v0.2 in the `.md` under entries 088 and 089; their `.docx` renderings remain the v0.1 text and no longer carry equivalent information. The same already applies to `CMP-Implementation-Tracker.docx`, which Index §3A records as generated from the workbook and never edited. | CMP-DOC-13, CMP-DOC-19, `CMP-Implementation-Tracker.docx`, README §8.2, Index §4, §3A | The `.md` is the source of truth (`CLAUDE.md` §3) and carries the change in full, so no requirement, statement or decision is lost. The `.docx` presentations are stale until regenerated, and anyone reading only a `.docx` will not see `SEC-241`, `SEC-242`, `OPS-209` or `OPS-210`. | Project Owner | **Pending.** No project process authorises Claude Code to generate or regenerate a `.docx`, and the Project Owner’s instruction of 2026-08-20 directs that regeneration be performed **only if the existing project process explicitly permits it, and otherwise reported as outstanding**. It is reported, not performed. | Open |
| CC-029 | 2026-08-20 | **`BE-173` asks the evidential record for a value it has no field for.** `BE-173` requires a policy change to be evidenced *"with actor, previous value and new value"*. `BE-107` ‡ fixes the evidential record at six fields — actor, action, subject, time, outcome, reason — and `BE-201` ‡ is the reason there is no seventh: a free-form context field is where a payment credential, a precise location or a contact detail arrives. There is therefore no field for either value, and none may be added lightly. Surfaced during `CMP-IMP-031`’s evidential wiring on 2026-08-20 and reported rather than resolved by the agent. | CMP-DOC-09 §9 (`BE-107` ‡, `BE-173`, `BE-201` ‡), CMP-DOC-11 (`DB-109` ‡, `DB-152`, `DB-154`), CMP-DOC-07 (`ARCH-115`) | Either the evidential record gains a seventh field, against `BE-107` ‡ and `BE-201` ‡, or `BE-173` is read as discharged by the versioned record and the evidential record **together**. Left unresolved, a policy change is either under-evidenced or evidenced through a field the security design forbids. | Project Owner | **The narrow reading is accepted (Project Owner, 2026-08-20).** `BE-173` is satisfied by the combination of: (a) `cfg_policy_versions` holding the previous and new values, as `DB-152` requires; (b) the evidential record naming the actor and pointing at exactly one of those version rows through its subject, `<key>@v<version>`; and (c) the six-field evidential record **remaining unchanged**. **No seventh field is to be added to the evidential record, and CMP-DOC-09 §9 is not to be modified** unless a future authoritative decision explicitly requires the values themselves to be stored inside it. The reasoning stands recorded so that it is not re-derived silently: `BE-107` ‡ and `BE-201` ‡ are integrity-critical and `BE-173` is not, so where they pull against one another the ‡ statements decide. Realised at `ChangePolicyValue::subjectFor()` and asserted by `PolicyStoreTest::test_the_record_points_at_the_one_version_holding_the_values()`. | Closed |
| CC-030 | 2026-08-20 | **The framework answers an unrouted method before the interface does.** `API-071` ‡ requires every failure to be represented as exactly one of four branches. The routing layer answers a request naming a method it does not route on a known path with `405`, and an unknown path with `404`, **before** any interface middleware or error mapping runs — so neither response passes through `FailureResponse` and neither carries the four-branch shape. Observed while testing `CMP-IMP-436` on 2026-08-20. | CMP-DOC-10 §8 (`API-071` ‡, `API-072` ‡, `API-094` ‡), §8.6, `API-004` | Either `API-071` ‡ is read as covering a request that names no operation — in which case the surface needs a fallback handler catching every unrouted request and reshaping it — or it is read as governing the failure of an **operation**, in which case a request naming none is outside its scope and the framework’s response stands. | Project Owner | **The narrower reading is accepted (Project Owner, 2026-08-20).** `API-071` ‡ governs the failure of an operation; a request naming no operation is not an operation failing. Recorded as **`API-219`** in CMP-DOC-10 v0.2. The Project Owner directed that **no fallback handler be introduced** and that no speculative infrastructure be added solely to eliminate the observed `405`, and that the position be revisited through change control should an authoritative specification later establish that `API-071` ‡ reaches unrouted methods. The behaviour and the reading are recorded at `IdempotencyKeyTest`, which is where the decision would attach. | Closed |
| CC-031 | 2026-08-20 | **Registration with a number held by a suspended or deactivated account has no specified behaviour.** `UC-001` step 4 validates that the number *"is not already registered to an active account"* and offers three outcomes: A1 the number is in use, offer the login path; A2 registration was abandoned before verification, the person may restart with the same number; E1–E3 invalid details, failed verification, messaging unavailable. **None covers a number held by a `SUSPENDED` or `DEACTIVATED` account.** The case did not exist when `UC-001` was written, because `BAD-RULE-010` was `[TBD – BAD-DEC-006]` until 2026-08-20. | CMP-DOC-03 `UC-001` (steps 4–5, A1, A2), CMP-DOC-04 `FRD-FR-004`, `FRD-FR-013`, `FRD-FR-018`, CMP-DOC-01 `BAD-RULE-010`, CMP-DOC-11 integrity constraint 22 | Neither available outcome is safe. **Reuse** would hand a suspended or deactivated person a working registration path back into the platform, which `BAD-RULE-010` forbids — it permits regaining an active session only *"through a defined account-state transition"*, and none is defined (`FRD-GAP-024`). **A second account** is forbidden by the Project Owner’s ratification of 2026-08-20 and is in any case prevented by `op_users_phone_number_unique`. So a registration attempt on such a number currently has **no specified outcome at all**, and `FRD-RISK-002` — a developer implements a gap — is the risk of leaving it to whoever writes the registration service. | Project Owner | **Pending.** Reported rather than resolved: choosing an outcome would be inventing account-lifecycle behaviour, which `BAD-DEC-006` still owes for transitions and `BAD-DEC-016` for appeal. The registration work item is not built, so nothing depends on this yet; it must be decided before `FRD-FR-001`–`FRD-FR-004` are implemented. | Open |

## 6. Superseded / Deprecated Register

| Document ID | Document | Version | Superseded/Deprecated On | Replaced By | Reason |
|---|---|---|---|---|---|
| — | — | — | — | — | No superseded or deprecated documents. |

---

*End of Document Change Log — CMP*
